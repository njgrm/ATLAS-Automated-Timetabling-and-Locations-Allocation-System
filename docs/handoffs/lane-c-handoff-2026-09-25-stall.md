# Lane C handoff: server stalls freeze draft manual edits (2026-09-25, evening)

Start a **fresh session** from this file (context economy, `AGENTS.md` §16). Read `AGENTS.md` §6, §11, §13 and
the `Live release` block of `docs/plans/live-state.md` on `origin/main`.

## State (verified 2026-09-25 17:30 local)

- **Live: `ad8f9717`** (C04 swap/scroll fixes + C05 published teacher change moves Teaching Load). Deployed and
  verified; browser acceptance unperformed (see below). Rollback basis `82871619`.
- **Run 318** (draft, generated by the operator before the demo) is what `/timetable` opens. Run 317 is still the
  published run. The operator says to use 318.
- `E:` release-directory reclaim: Lane A froze "manifest B" on `main` (`43d118bd`). Lane C does not duplicate it.
- Worktrees: `E:/ATLAS-worktrees/lane-c-demo-departure-swap` (branches `fix/demo-departure-swap`,
  `fix/departure-load-transfer`, both integrated) → `RETIRE_AFTER_INTEGRATION`.
  `E:/ATLAS-worktrees/lane-c-server-stall-c01` (`work/lane-c-server-stall-c01`, SERVER-STALL-C01 `c198cd9`
  accepted 5/0/0, **not integrated**) → `KEEP_ACTIVE`.

## The operator's report: "manual draft placements are very broken"

Evidence gathered (Chrome, seeded session, run 318, GR7 Luna Term 2):

1. Select Tue MATH → "Choose a new time" → Thu 10:45 (occupied): the draft swap preview
   `POST …/runs/318/manual-edits/swap/preview` took **31,696 ms**, and the supervisor log shows
   `[event-loop-stall] blocked ~31522ms` for that request. The **whole server freezes** for every user while
   it runs. The preview result itself was correct ("Safe to review").
2. Every page load: `GET …/runs/latest/draft` 8–9 s with a ~7.7–7.9 s `[event-loop-stall]` (the known
   `/timetable` stall), same in `e8553752`, `ff87b06b` and `82871619` logs.
3. **Offline, the same code is fast** (read-only scripts against the live DB, using the release's built `dist`,
   `node --env-file=D:/ATLAS-runtime-config/atlas-server.env`):
   - `loadRunContext(318)` 323 ms; `validateHardConstraints` over 2,760 entries **83 ms** (332 violations)
   - `previewManualSwapEntries(318, 'entry-369::t2', 'entry-669::t2')` **406 ms**
   - `getLatestRunDraft(1,10)` 700 ms cold / 163 ms warm (1.8 MB response)
4. Live server process: 103 MB working set (not a heap problem). About 12 established connections, several
   `GET /api/v1/notifications/:n/events` SSE streams open for 15+ minutes.

**Conclusion so far:** the blocking CPU is not in the manual-edit, validation or draft code paths. Something
else inside the live process blocks the loop while those requests are in flight. The stall line cannot name it
because open SSE streams fill the `active:` list. That is exactly what SERVER-STALL-C01 (`c198cd9`) fixes: it
lists streams as a count and adds `heap=`.

## Next actions, in order

1. **Integrate SERVER-STALL-C01** (`work/lane-c-server-stall-c01`, already `ACCEPT_READY`): merge `--no-ff` on a
   clean `integration/*` from current `origin/main`; `npm run test:request-timing` + server build + Node start on
   an isolated port; push. Deploy it (operator runs the elevated `deploy-runner.ps1`; incumbent `ad8f9717`).
2. Reproduce once (one draft swap preview on run 318) and read the new stall line: it will name the blocking
   request without the SSE noise. Suspects to check first: the SSE notification handlers (per-connection
   polling/serialization), `authenticate` middleware, `runtime/context?verifyUpstream=true` (4 s EnrollPro
   timeout; sync work around it), `readiness/diagnostic`.
3. Open the root-cause fix with a failing-first test and one fresh review (MEDIUM; HIGH only if it touches auth).
4. Then ask the operator what else was "broken" in manual placements; the 30 s freeze may be all of it, or not.
5. Browser acceptance for `ad8f9717` on whatever run is published at the time: B2 (a MATH teacher is refused
   with the plain message) and, with the operator's standing approval, B1 (one teacher-leaving commit, record
   Teaching Load owners before and after; baseline 2026-09-25: Tolentino (38) owns subject:section 8:151, 9:142,
   10:142, 9:153, 10:153).

## Cost notes for the next session

- Run browser QA in a subagent (Sonnet) so its many small steps do not grow the main context.
- Use Haiku/Sonnet for Explore searches and MEDIUM reviews; keep Opus for HIGH reviews.
- The scratch profiling scripts live in this session's scratchpad only; recreate them if needed (pattern above).
