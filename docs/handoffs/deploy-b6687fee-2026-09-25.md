# Deploy handoff — release `b6687fee` (2026-09-25)

Evidence moved out of the `## Live release` block of `docs/plans/live-state.md` per §15
(keep SHA / directory / listeners / rollback basis / acceptance owner there).

## `b6687fee` — scheduler-clarity client release (LIVE 2026-09-25)

- Target SHA `b6687feed22dbfdf47d71d08e1e2867ce9f613ea`; release dir
  `E:\ATLAS-runtime-supervised-b6687fee-20260925`; rollback basis `066da7a7`.
- Range `066da7a7..b6687fee`: client + docs only — **no `atlas-server`/`prisma` change, no migration**.
- Handoff: Luna's candidate + Terra's independent review `ACCEPT_READY`; route/state preservation 58/58;
  scheduler-clarity suite 52/52; production client build passed.
- Cutover by `ops/runtime/deploy-runner.ps1`: dry-run audit
  `C:\ProgramData\ATLAS\release-audit\b6687fee-20260925-052343`; execute audit
  `C:\ProgramData\ATLAS\release-audit\b6687fee-20260925-052413`.
- Verified: machine `ATLAS_RUNTIME_SOURCE_DIR`/`RELEASE_SHA` = target; listeners 5001→70176 /
  5174→80088; health/ready (`database:"ok"`) + `GET /api/v1/subjects?schoolId=1` 200 local & Tailnet.
  Served entry `assets/index-DNzys7Zh.js`
  (SHA-256 `C11F747CDB98F2F4608C2AA0E86E56A2A49D17052FAAC25CD9294E11C767C851`) and new-build-only chunk
  `assets/ScheduleReviewWorkspace-DPx-KMEP.js`
  (SHA-256 `9844683E9B781AA5EE910713F9B065F2AB3D9E3932A48168B9EDBD36A8FCFE26`) both byte-identical to
  the build and absent from the incumbent.
- Fresh independent post-cutover QA: `ACCEPT_READY` 7/7/0/0.
- Browser acceptance is separate and not performed by the cutover.

## `066da7a7` — teacher-concern C5+C6 (LIVE 2026-09-25 → rollback basis)

- Release dir `E:\ATLAS-runtime-supervised-066da7a7-20260925`; supervisor-owned 5001→78160 / 5174→74512.
- Migration `20260925000002_faculty_availability` applied via the guarded runner to
  `atlas_recovery_clean_rebuild_20260905` (`MIGRATE_GATE_OK`; backup
  `atlas-backup-atlas_recovery_clean_rebuild_20260905-20260924-204504.dump`, sha256
  `1E6343C75E1FADC73F090898A44E34347494A64FB329203F44E518133EFFA74A`; applied count 10 → 11).
- Served entry `assets/index-D90Rg0kl.js` (SHA-256
  `6C057F4C66A235183D35C9448C3DAD86934DDECEDAB7092B54FA0D152B8EC44C`) and new-build-only chunk
  `assets/TeacherConcerns-B1glPilv.js` (SHA-256
  `834649FF94880B4BD779570A3749BDA82AB1D5F41DAE1E06098E0BF48BD41AEA`).
- Cutover audits `…\release-audit\066da7a7-20260925-044557` (dry-run) / `…-044622` (execute). Fresh
  post-action QA `ACCEPT_READY` 8/8/0/0.
- **Acceptance INCOMPLETE** — tally `passed 0 / blocked 1 / unperformed 3 / NEEDS_SESSION 0`. Blocked on
  `409 TERM_SCOPE_MISMATCH` (client `resolveActiveSchoolYearContext` vs the server's persisted active
  term) → corrective lane `AVAILABILITY-TERM-ALIGNMENT-C01`. The collaboration-ticket `201` incident is
  closed: the endpoint issues a 60 s in-memory single-use ticket (`timetable-collaboration-ticket.service.ts`),
  no shared-data mutation. Packet: `docs/prompts/c7-teacher-concern-deploy-2026-09-25.md`.

## Process note (for every agent running QA)

Stop an isolated test server by the PID that is **actually listening on the port** (e.g.
`Get-NetTCPConnection -State Listen -LocalPort <port>`), not by a wrapper/shell PID — `kill $!` leaves the
`node` child running. A leftover `node dist/server.js` on 5199 held a file lock and blocked a worktree
deletion. Check for stray listeners before retiring a worktree.

## `89295c27` — server-timing diagnostics (LIVE 2026-09-25)

- Range `b6687fee..89295c27`: server (`SERVER-TIMING-C01`) + test + docs — no `prisma`/migration; client
  bytes unchanged.
- Cutover audits: dry-run `C:\ProgramData\ATLAS\release-audit\89295c27-20260925-055104`; execute
  `…\89295c27-20260925-055126`.
- Listeners 5001→76676 / 5174→53516; health/ready + DB-backed read local & Tailnet 200; target
  `atlas-server/dist/lib/request-timing.js` present, absent from the incumbent.
- Fresh post-cutover QA: runtime/build/migration/write/residue rows all PASS; one docs-only BLOCKING
  (audit paths embedded in the `Live release` block) corrected here — see this file.
- Stall diagnostics left in `<targetDir>/ops/runtime/logs/atlas-supervisor.log` for Lane C (Lane C owns
  attribution; the stall is NOT fixed).
- Earlier `37e0c85b` cutover audit: `C:\ProgramData\ATLAS\release-audit\37e0c85b-20260925-021830`.
- Process note: `npm ci` is denied by the agent harness deny-list; this release's dependency tree was
  staged by copy from the lockfile-verified incumbent (own tree, no junction, lockfiles byte-identical).
