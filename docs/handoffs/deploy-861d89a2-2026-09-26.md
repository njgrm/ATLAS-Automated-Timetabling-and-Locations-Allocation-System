# Deployment handoff — `861d89a2` ACTIVE-TERM-LIVE-RESOLUTION-C02 (resumed executor, 2026-09-26)

Verdict: **REVIEW_REQUIRED** (executor return; not `ACCEPT_READY`). Deployment executed under the
operator's 2026-09-20 standing authorization after the packet gates. Q4 (authenticated readiness pair),
Q7 (independent focused controls) and Q8 (acceptance honesty) are **not** closed here: one fresh
read-only `atlas-qa` owns them. No migration, generation, publication, browser action, or live-data
write occurred.

- Packet: `docs/prompts/deploy-861d89a2-active-term-c02-2026-09-25.md` (read at `01b0a62f`; byte-identical at
  this branch's `7814bfa0`).
- Incumbent / rollback at start: `eb0e30386336673a4a31ecfe39a9bef93549e0ed` at
  `E:\ATLAS-runtime-supervised-eb0e3038-20260925`.
- Target product / live release after cutover: `861d89a2bc2682c5f875dde0b4b1d8ffc079b1fe` at
  `E:\ATLAS-runtime-supervised-861d89a2-20260925`.
- Candidate commit (this handoff only): branch `docs/lane-a-r1-deploy-target`, one additive docs file.

## 1. Resumed state (recorded, not re-created)

The previous executor was interrupted after provisioning the target and building the server. Nothing was
duplicated and no cutover was re-run.

| Resumed item | Evidence |
|---|---|
| Target worktree added, detached at exact SHA | `git -C E:\ATLAS-runtime-supervised-861d89a2-20260925 rev-parse HEAD` = `861d89a2bc2682c5f875dde0b4b1d8ffc079b1fe` |
| Donor dependency trees already satisfy the donor checks | lockfile SHA-256 donor vs target equal for all three: root `9332EF25659983DA…`, `atlas-server` `76061F55843DB6F8…`, `atlas-client` `ACEF8E370AFF3F6B…`; `node_modules` present as **real directories** (not reparse points) with matching child counts root 217 / server 209 / client 155; donor `5c100ea6d385d162189ffceca0479db5793ef611` clean. **No recopy performed.** |
| Prisma client already generated | `atlas-server/node_modules/.prisma/client` newest file `2026-09-26 01:12:55` |
| Server already built | `atlas-server/dist/server.js` `2026-09-26 01:13:17` vs newest source `01:08:49`; C02 marker `resolvePreResolvedActiveTermAuthority` present in `dist/services/academic-term.service.js:251` |
| Client `dist` absent at resume | `Test-Path atlas-client/dist` = `False`; built in this session |
| No `861d89a2` release audit existed | `C:\ProgramData\ATLAS\release-audit` held no `861d89a2-*` directory before this run |

## 2. Pre-action identity re-derivation (read-only, no drift)

| Check | Result |
|---|---|
| Task action | `node "E:\ATLAS-runtime-supervised-eb0e3038-20260925\ops\runtime\cli.mjs" start`, `Start In` = same, `Status: Running` |
| Machine env | `ATLAS_RUNTIME_SOURCE_DIR=E:\ATLAS-runtime-supervised-eb0e3038-20260925`, `ATLAS_RUNTIME_RELEASE_SHA=eb0e3038…`, `ATLAS_RUNTIME_ENV_FILE=D:\ATLAS-runtime-config\atlas-server.env` |
| Authoritative state file | `E:\ATLAS-runtime-supervised-eb0e3038-20260925\ops\runtime\logs\supervisor-state.json` → `state=running`, `releaseSha=eb0e3038…`, `sourceDir=…eb0e3038…`, `ownedPids {server 85488, client 81024}` |
| Listeners / lineage | 5001→85488, 5174→81024; single parent 78080 = `…eb0e3038…\ops\runtime\cli.mjs start` |
| Capacity | E: **48.03 GiB** free, D: **60.67 GiB** free. Above both fail-closed lines (E 25, D 15). **No second reclaim run** — packet §3 records the retention obligation as discharged for this revision and forbids a self-rearming second reclaim. |
| Live-state gate | `docs/plans/live-state.md` at `origin/main` names pending release `861d89a2bc2682c5f875dde0b4b1d8ffc079b1fe` inside `## Live release` |

Stale-state trap avoided: this session's own process env carried `ATLAS_RUNTIME_SOURCE_DIR=E:\ATLAS-runtime-supervised-c5e167d7-20260925`.
`cli.mjs status` therefore reported the `c5e167d7` state file (`live:false`, PIDs `54256/77492`) — **not** the running
supervisor. Identity was taken from the machine env, the task action, and the state file of the directory the task
actually runs. The ambient session variable was never mutated.

## 3. Build and control gates (literal commands, exact tallies)

All commands run in `E:\ATLAS-runtime-supervised-861d89a2-20260925` unless stated.

| Gate | Literal command | Tally / result |
|---|---|---|
| C02 unit | `npm run test:active-term-live-resolution-c02` | **13 pass / 0 fail** (13 tests) |
| C02 PostgreSQL (disposable) | `npm run test:active-term-live-resolution-c02-postgres` | **6 pass / 0 fail** (6 tests); harness provisions and force-drops its own guarded database and asserts zero residue |
| C01 supersession | `npm run test:active-term-live-resolution` | **8 pass / 0 fail** (8 tests) |
| Server build (rebuilt this session) | `npm run build` (`tsc`) | exit 0; `dist/server.js` SHA-256 `5018571A50E695AEBAE359057580BA2D13BBBAD152388534AE6D91C191817F21` |
| Client typecheck | `npm run typecheck` (`tsc --noEmit -p tsconfig.json`) | exit 0 |
| Client build | `$env:VITE_ENROLLPRO_URL="https://dev-jegs.buru-degree.ts.net"; npm run build` | exit 0; entry `index-BW9wl1o-.js` SHA-256 `40E586453038B7E9B3C6D76C9BCE011BA80F8872E98418738342EBB1FE294BC1`; CSS `index-C3v4WoAk.css` `56CD6770DF8FF4D1512B1B8384B20BD936C15A8C8729FA6754688FB771BE765C` |
| Client term-gate | `npx --no-install tsx --test --test-reporter=tap src\lib\__tests__\timetable-term-gate-c01.test.ts` (in `atlas-client`) | **8 pass / 0 fail**; the file is byte-identical at `eb0e3038` (no client path changed in the range) |
| 13-file server preservation subset | see below | target **47 pass / 17 fail (64 tests)**; base `eb0e3038` **46 pass / 17 fail (63 tests)** — identical failure set, **0 candidate-only failures** |

Preservation subset literal command (run in `atlas-server`; `--test-reporter=tap` added so per-test identity is
reproducible, the same 13 files and nothing else):

```
npx --no-install tsx --test --test-reporter=tap src\__tests__\tt-source-freshness-generation-c04.test.ts src\__tests__\tt-source-freshness-capability-c04.test.ts src\__tests__\tt-source-freshness-quick-place-c04.test.ts src\__tests__\tt-source-freshness-sync-pin-c04.test.ts src\__tests__\publication-contract-readiness.test.ts src\__tests__\published-revision-identity-s4.test.ts src\__tests__\published-identity-readback-s4-client.test.ts src\__tests__\published-swap-term-guard-c04.test.ts src\__tests__\publication-contract-postgres-concurrency.test.ts src\__tests__\term-contract-atlas-consumption-c02.test.ts src\__tests__\term-subject-authority.test.ts src\__tests__\generation-authority-realism-c07-term-authority.test.ts src\__tests__\active-term-live-resolution-c01.test.ts
```

The 17 failures are **base-reproduced** in this environment: the same 16 named tests (`S1`, `S2`×5, `Q1`, `Q2`×3,
`Q3`, `C1`–`C5`) plus the file-level failure of `publication-contract-postgres-concurrency.test.ts`, all failing
with `409 GENERATION_PREFLIGHT_BLOCKED` / a `DATABASE_URL`-unset Prisma init, at the target and at `eb0e3038`
alike. The one-test difference (64 vs 63) is the C02-added `N1 replacement (…C02)` test in
`active-term-live-resolution-c01.test.ts`. Re-running the target set with `DATABASE_URL` exported from the durable
env changed nothing (still 47/17), so the failures are not a missing-child-env artifact. The interrupted
executor's own run (`c02-preservation-target.txt`, 2026-09-25T17:14:58Z) recorded the same
`GENERATION_PREFLIGHT_BLOCKED` failures — independent corroboration.

## 4. Isolated start on port 5198 (never 5001/5174)

`dist/server.js` was started directly with the durable env composed into the child environment plus, explicitly,
`PORT=5198` and `ROLLOVER_AUTO_SYNC_ENABLED=false` (without the flag the direct start arms an unattended rollover
tick against the live `DATABASE_URL` 300 s after `listen`).

- Listener PID `68868` on `127.0.0.1:5198`; `GET /api/v1/health` **200**;
  `GET /api/v1/health/ready` **200** body `{"status":"ready","service":"atlas","checks":{"database":"ok"}}`;
  `GET /api/v1/subjects?schoolId=1` **200**, 22 records.
- Startup log: `DATABASE_URL protocol looks correct`, `Server listening on http://localhost:5198`,
  `DB connected, 2 school(s) found`, `scheduling_policies schema verified`,
  `[rollover-automation] Disabled via ROLLOVER_AUTO_SYNC_ENABLED=false`; stderr empty.
- The PID actually listening on 5198 was stopped; `Get-NetTCPConnection -State Listen -LocalPort 5198` → **0**
  listeners afterwards. 5001/5174 were never touched by this step.

## 5. Zero-write digests (packet SQL, before and after cutover)

SQL file: `C:\Users\njgro\AppData\Local\Temp\opencode\atlas-zero-write-861d89a2.sql`, 894 bytes, UTF-8 **without
BOM**, **LF** line endings, the packet's literal bytes including `E'\\n'` (backslash-n, two characters) as written
in the packet file.

Literal command (adaptation recorded, per §11 "record what you actually ran"): the durable `DATABASE_URL` carries
the Prisma-only `schema` query parameter, which `psql` rejects, so that one parameter was removed and nothing else
changed.

```
D:\PostgreSQL\18\bin\psql.exe "<durable DATABASE_URL minus ?schema=...>" -X -q -A -t -F "|" -f C:\Users\njgro\AppData\Local\Temp\opencode\atlas-zero-write-861d89a2.sql
```

Database name: `atlas_recovery_clean_rebuild_20260905`. Values were never printed; only md5 digests.

| Table | PRE (file written 2026-09-25T17:26:07Z) | POST (2026-09-25T17:27:50Z) |
|---|---|---|
| `audit_logs` | `f10b4eb9c0a6fd63f2d48fabb08f596e` | `f10b4eb9c0a6fd63f2d48fabb08f596e` |
| `faculty_availabilities` | `a83264ed4d9cd22334a9cd7898cec3f1` | `a83264ed4d9cd22334a9cd7898cec3f1` |
| `faculty_availability_slots` | `d41d8cd98f00b204e9800998ecf8427e` | `d41d8cd98f00b204e9800998ecf8427e` |
| `generation_runs` | `594dcd9313b72e4e156cdfea4981cd84` | `594dcd9313b72e4e156cdfea4981cd84` |
| `manual_schedule_edits` | `d41d8cd98f00b204e9800998ecf8427e` | `d41d8cd98f00b204e9800998ecf8427e` |
| `published_schedule_revisions` | `38ac92168ccbd09c19683774ac6afb26` | `38ac92168ccbd09c19683774ac6afb26` |

`Compare-Object` between the pre and post files: **0 differing lines**. The POST capture was taken before any
browser or data action; no such action was performed at all. (These digests intentionally differ from the
`eb0e3038` cycle's 2026-09-25T14:40:36Z baselines for `audit_logs`, `generation_runs` and
`published_schedule_revisions`: the operator's draft run 318 and later QA logins legitimately added rows. Only
pre-vs-post equality is claimed here.)

## 6. Cutover (the runner was the only task/env/listener owner)

Both invocations used the packet's exact arguments, elevated, in the same PowerShell host:

```powershell
& "E:\ATLAS-runtime-supervised-861d89a2-20260925\ops\runtime\deploy-runner.ps1" `
  -TargetSha "861d89a2bc2682c5f875dde0b4b1d8ffc079b1fe" `
  -TargetSourceDir "E:\ATLAS-runtime-supervised-861d89a2-20260925" `
  -IncumbentSha "eb0e30386336673a4a31ecfe39a9bef93549e0ed" `
  -IncumbentSourceDir "E:\ATLAS-runtime-supervised-eb0e3038-20260925" `
  -EnvFile "D:\ATLAS-runtime-config\atlas-server.env"
```

Dry-run → audit `C:\ProgramData\ATLAS\release-audit\861d89a2-20260926-012619` (`deployment-plan.json`,
`task-before.xml`, `task-target.xml`): exact target and incumbent, `mutates: false`, `secretsPrinted: false`,
live-state gate passed, one lineage (`supervisorPid 78080`, `listenerPids [85488, 81024]`), rollback recorded.
Exit 0.

Same arguments plus `-Execute` → audit `C:\ProgramData\ATLAS\release-audit\861d89a2-20260926-012633`:
`{"result":"CUTOVER_STARTED","releaseSha":"861d89a2…"}`. Exit 0. No improvised rollback, no manual task edit,
no manual environment write, no direct listener action.

## 7. Live verification after cutover (Q1, Q2, Q3, Q5, Q6 only)

| Row | Evidence | Result |
|---|---|---|
| Q1 exact target live | Task action/Start In = `E:\ATLAS-runtime-supervised-861d89a2-20260925\ops\runtime\cli.mjs start`, `Status: Running`; machine `ATLAS_RUNTIME_SOURCE_DIR`/`ATLAS_RUNTIME_RELEASE_SHA` = target; state file `running` / `releaseSha 861d89a2…` / `sourceDir` = target directory / `startedAt 2026-09-25T17:27:03.766Z`; listeners 5001→36120, 5174→62504; single parent 85536 = target `cli.mjs`; children are the target `dist/server.js` and `ops/runtime/host.mjs` | **PASS** |
| Q2 local runtime | `/api/v1/health` 200; `/api/v1/health/ready` 200 `checks.database:"ok"`; `GET /api/v1/subjects?schoolId=1` 200, 22 records | **PASS** |
| Q3 Tailnet | `https://njgrm.buru-degree.ts.net/api/v1/health` 200; `/api/v1/health/ready` 200 `checks.database:"ok"` (Tailnet funnel → `127.0.0.1:5174`, as recorded before and after by the earlier probe) | **PASS** |
| Q5 startup evidence | `E:\ATLAS-runtime-supervised-861d89a2-20260925\ops\runtime\logs\atlas-supervisor.log`: `Starting RUNTIME-SUPERVISION-C01 … releaseSha=861d89a2…`; environment reference (17 keys, redacted values); `Launched targets: server=36120 client=62504`; `[client] Production host listening on port 5174 (artifact=…861d89a2…\atlas-client\dist)`; `[server] DATABASE_URL protocol looks correct`; `Server listening on http://localhost:***REDACTED***`; `DB connected, 2 school(s) found`; `scheduling_policies schema verified`; `[rollover-automation] Disabled via ROLLOVER_AUTO_SYNC_ENABLED=false`; `All targets healthy (liveness and dependency readiness).` No fatal startup, listener, or schema error. | **PASS** |
| Q6 zero live-data write | pre/post six-table digests identical, 0 differing lines (section 5) | **PASS** |
| Q4 deployed active-term behavior | **NOT PERFORMED by design** — the fresh post-action `atlas-qa` owns the authenticated cold/warm readiness pair | not performed |
| Q7 focused controls | **NOT PERFORMED** — independent rerun belongs to the fresh `atlas-qa`; this handoff records the executor's tallies as input | not performed |
| Q8 acceptance honesty | Cannot be `ACCEPT_READY` from this seat | not performed |

Served-asset parity: the Tailnet-served `assets/index-BW9wl1o-.js` SHA-256 is
`40E586453038B7E9B3C6D76C9BCE011BA80F8872E98418738342EBB1FE294BC1`, byte-identical to the build in
`E:\ATLAS-runtime-supervised-861d89a2-20260925\atlas-client\dist`.

## 8. Risks

- **NON_BLOCKING** — 17 preservation-set failures, all base-reproduced with an identical failure set; the packet
  asserts no fixed preservation total. The independent `atlas-qa` must reproduce both runs before it can accept
  Q7; it must also use the same literal command and record its own tallies.
- **NON_BLOCKING** — the pre-cutover digests differ from the `eb0e3038` cycle's accepted baselines on three tables
  (draft run 318, QA logins). Only pre-vs-post equality is claimed; a later comparison against the older baseline
  must re-derive at that moment.
- **NON_BLOCKING** — one literal deviation from the packet's SQL invocation: the Prisma-only `schema` query
  parameter had to be removed from the `psql` URL. The SQL text itself is the packet's bytes.
- **NON_BLOCKING** — the base reproduction of the preservation set was executed inside the rollback release
  directory `E:\ATLAS-runtime-supervised-eb0e3038-20260925\atlas-server` (no dependency copy, no `npm ci`); it
  wrote no file there and touched no live table (`git status` there is unchanged: only the pre-existing
  `?? ops/runtime/logs/`). A reviewer may prefer a dedicated base worktree.
- **NON_BLOCKING** — `docs/plans/live-state.md` on `origin/main` still shows `eb0e3038` as **LIVE** with `861d89a2`
  as the pending release. Updating the live-release record is the integration owner's action, not the executor's.
- **BLOCKING for acceptance, not for the deployment** — Q4, Q7, Q8 remain open and no generation, publication,
  availability/Teaching Load write, term-cache apply, or rollover sync is authorized by this cycle.

## 9. Cleanliness and worktree dispositions

- `E:\ATLAS-runtime-supervised-861d89a2-20260925` — HEAD `861d89a2…`, `git status --short` = `?? ops/runtime/logs/`
  only (the running supervisor's own log/state directory, identical to the incumbent's). No tracked-file change;
  this is the state the runner's clean gate also saw. → **KEEP_ACTIVE** (live release).
- `E:\ATLAS-runtime-supervised-eb0e3038-20260925` — unchanged HEAD `eb0e3038…`, status `?? ops/runtime/logs/`.
  → **PRESERVE_FOR_DECISION** (rollback basis).
- `E:\ATLAS-runtime-supervised-5c100ea6-20260925` — donor, clean, untouched. → **PRESERVE_FOR_DECISION**.
- `E:\ATLAS-worktrees\lane-a-r1-deploy-target` — this handoff is the only added file. Pre-existing stashes
  (`work/section-route-authority-c01` 2026-09-19, `fix/scheduler-search-c01` 2026-09-18,
  `work/flag-window-per-scope-c01` 2026-09-18) belong to other lanes and were neither created nor touched here.
  → **KEEP_ACTIVE** until the planner integrates this handoff and the live-state update.
- Temporary artifacts live only in `C:\Users\njgro\AppData\Local\Temp\opencode`
  (`atlas-zero-write-861d89a2.sql`, `zw-pre-861d89a2.txt`, `zw-post-861d89a2.txt`, `pres13*.txt`, `termgate.txt`,
  `iso5198*.txt`, `client-build.txt`, `client-tsc.txt`, `server-build.txt`, `served-entry.js`).
  No helper script was left in the repository. No stash, no reset, no rebase, no push, no merge.
