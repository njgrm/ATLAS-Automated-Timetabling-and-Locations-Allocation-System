# Deployment handoff — eb0e3038 READINESS-STALL-C01 (2026-09-25)

**Outcome: `DEPLOYED_ACCEPTANCE_INCOMPLETE`.** The supervised cutover completed; post-action
acceptance is blocked only at Q4 because the available seeded QA profile has no authenticated
session. No generation, publication, migration, or timetable-data write occurred.

## Cutover

- Target: `eb0e30386336673a4a31ecfe39a9bef93549e0ed`
- Target release: `E:\ATLAS-runtime-supervised-eb0e3038-20260925`
- Rollback/incumbent: `c5e167d7c5939ff586880149c566ce29506430e8`
- Rollback directory: `E:\ATLAS-runtime-supervised-c5e167d7-20260925`
- Dry-run audit: `C:\ProgramData\ATLAS\release-audit\eb0e3038-20260925-221651`
- Execute audit: `C:\ProgramData\ATLAS\release-audit\eb0e3038-20260925-221948`
- Live task/env/state/listeners: target `running`; one target supervisor lineage; ports 5001/5174.
- Health, ready with `database:"ok"`, DB-backed subjects read, Tailnet health, and startup log: PASS.

## Build and controls

- Server build: PASS; readiness-stall 7/7; request-timing 6/6; server suite 337/341 with exactly the four recorded pre-existing `tt-output-c03r` failures.
- Client typecheck/build: PASS. Server-only target retains the incumbent client asset hash.
- Isolated 5198 startup: health/ready/subjects PASS; listener PID stopped and port clear.
- No migration/schema/SQL/lockfile path in the target range.

## Q4 blocker (initial attempts)

The required authenticated cold/warm pair was attempted twice by fresh QA using the available seeded
profile, without login and without navigating `/timetable` first:

- `GET /api/v1/generation/1/10/readiness/diagnostic` call 1: HTTP 401 `NO_TOKEN`
- call 2: HTTP 401 `NO_TOKEN`

No response body, scheduler field, or zero-write assertion was available. Supervisor log remained
unchanged at 1989 bytes. Required result before retry: `NEEDS_SESSION(atlas-qa/primary-seeded-profile)`
or another explicitly seeded QA profile. Do not infer cache behavior or unlock A1.

## Q4 resolution (operator-seeded session)

The operator seeded the Lane A browser session and confirmed login. The valid harness stayed on the
same-origin root page and did not navigate to `/timetable`; it used the browser's existing local token
without recording or printing its value. Two authenticated requests to
`GET /api/v1/generation/1/10/readiness/diagnostic` returned:

| Call | HTTP | Wall time | Scope | `scheduler.ran` | `scheduler.cached` | `runtimeMs` | `databaseSignature.zeroWrite` | Status |
|---|---:|---:|---|---|---|---:|---|---|
| 1 | 200 | 2397 ms | school 1 / year 10 | true | false | 1904 | true | READY |
| 2 | 200 | 388.8 ms | school 1 / year 10 | true | true | 13 | true | READY |

The first call produced the expected cold scheduler work; the second reused the bounded cache. The
supervisor log recorded the cold readiness request as a 2166 ms event-loop stall / 2390 ms slow request;
the warm call added no new readiness stall line. The two earlier 401 attempts did not reach the route or
consume the cache. This is planner-owned session evidence pending the fresh QA review of the artifact.

## Q6 correction

The executor handoff contained a malformed 33-character transcription for `audit_logs`. Fresh QA
re-ran the exact packet SQL at 2026-09-25 14:40:36Z (database `atlas_recovery_clean_rebuild_20260905`)
and matched all six valid accepted baselines:

- `audit_logs` `018a0236970088b257123fe3feef8ad7`
- `generation_runs` `efb22a7152b22ec5c7b5c445e1a5ece9`
- `manual_schedule_edits` `d41d8cd98f00b204e9800998ecf8427e`
- `published_schedule_revisions` `49210d022efabfaca3fc291d9dce4741`
- `faculty_availabilities` `a83264ed4d9cd22334a9cd7898cec3f1`
- `faculty_availability_slots` `d41d8cd98f00b204e9800998ecf8427e`

The malformed executor string is preserved as a transcription defect, not used as evidence.

## Pending acceptance

1. Seed an authorized QA profile with a valid session.
2. Fresh QA repeats exactly two credential-inclusive readiness calls, first `cached=false`, second
   `cached=true`, with no intervening readiness call.
3. Only then Lane C takes the supervisor-log cursor and performs A1's two warm `/timetable` reloads.

Target release is `KEEP_ACTIVE`; rollback is `PRESERVE_FOR_DECISION`; donor is `PRESERVE_FOR_DECISION`.
