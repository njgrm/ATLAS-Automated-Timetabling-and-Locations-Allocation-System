# WF-C04 — atomic stream registration and live-lane reconciliation: executor handoff

**Role:** EXECUTOR. **Risk tier:** MEDIUM (source/test + machine-registry
reconciliation, docs-only registry mutation via tooling). **Verdict:**
`REVIEW_REQUIRED`. This is the executor's single handoff for the candidate; it is
not approval, integration, or a `GO`. The executor does not self-accept, merge,
rebase, push, or plan successors.

## 1. Immutable identity

| Field | Value |
| --- | --- |
| Accepted base SHA | `9b82a98f7c74ffecd34e2dae337d7a69c19d7fdf` (fresh `origin/main`, re-fetched at execution start; observed tip identical) |
| Branch | `work/workflow-atomic-stream-registration-c04` |
| Worktree | `E:/ATLAS-worktrees/workflow-atomic-stream-registration-c04` |
| Frozen candidate | the tip commit of this branch carrying this file; see the executor return message for the exact 40-hex SHA |
| Directive pin | `AGENTS.md` LF-normalized SHA-256 `5f9206708a4763376dda1943c1ead28f49427ed1b1f0532ad25661f74ed3ebb5` (recomputed from both `origin/main:AGENTS.md` and the worktree copy; matched) |
| Governing packet | `docs/prompts/workflow-atomic-stream-registration-wfc04-2026-09-15.md`; blob `5659c9a7a2d095f4e846682016765fae708235ae` at `058ad4cb` (re-hashed with `git hash-object`; matches the planner's pin) |
| Worktree disposition | `RETIRE_AFTER_INTEGRATION` (not retired here) |

## 2. Changed-path inventory

Product / tooling / tests:

- `ops/workflow/lib/transition.mjs`
- `ops/workflow/transition.mjs`
- `ops/workflow/README.md`
- `ops/workflow/__tests__/create-stream.test.mjs` (new)
- `ops/workflow/__tests__/seed.test.mjs`
- `ops/workflow/specs/register/BENEFICIARY-EXPORT-PARITY-C05.json` (new; reviewed tooling input)
- `ops/workflow/specs/register/COMPANION-SSO-LIVE-PREP-C02.json` (new; reviewed tooling input)
- `ops/workflow/specs/register/TL-OPERATOR-WORKSPACE-C05.json` (new; reviewed tooling input)
- `ops/workflow/specs/register/WF-C04.json` (new; reviewed tooling input)

Registry (mutated only through `ops/workflow/transition.mjs`):

- `docs/plans/atlas-delivery-cycles.json` (revision 17 -> 22 via 5 tooling runs)
- `docs/plans/atlas-active-delivery-streams.generated.md` (regenerated by the same runs)

Handoff:

- `docs/handoffs/wf-c04-executor.md` (this file)

No `.gitattributes`, `package.json`, `.opencode/**`, `atlas-client/**`,
`atlas-server/**`, `docs/plans/atlas-active-delivery-streams.md`, or
`docs/prompts/**` change. Nothing was pinned as a byte-hashed artifact (section 7).

## 3. Requirement -> production path -> negative control -> verification command

| Requirement | Production path | Negative control | Verification command | Result |
| --- | --- | --- | --- | --- |
| Atomic `create-stream` in the existing pipeline (no parallel CLI/registry) | `ops/workflow/lib/transition.mjs` `scope: "create"` inside `runTransition`; CLI `ops/workflow/transition.mjs` | `--stream` rejected (`TRANSITION_FLAG_NOT_APPLICABLE`); no second entry point exists | `node ops/workflow/transition.mjs --transition create-stream ...` | PASS |
| Lock + expected-revision CAS + schema + semantic verify + render + render-byte verify + atomic replace | same `runTransition` body as every other transition | stale revision; held lock; `VERIFY_RENDERED` fault | `in-process` + CLI runs; fault matrix | PASS |
| Bounded input: exactly one complete stream record | `loadStreamSpec` -> `validateStreamSpec` (shipped `$defs.stream`) | unknown key (top-level and nested); non-object; malformed JSON; schema-incomplete | reject matrix | PASS |
| Remote tip is flag-sourced, never spec-asserted | `--observed-origin-main` -> `git.remoteObservation` | non-null spec `remoteObservation` rejected; malformed/unknown SHA rejected | reject matrix | PASS |
| Duplicate id rejected; retry never double-creates | duplicate gate before append + verifier `DUPLICATE_STREAM_ID` | repeat the same successful operation | retry control | PASS |
| Unknown / contradictory Git identity rejected | candidate document re-verified by `verifyStateDocument` | unknown SHA, broken ancestry, `changedPaths` mismatch | reject matrix | PASS |
| HIGH cannot bypass approval/dependency rules | same verifier | HIGH `execution.performed=true` with incomplete approval | HIGH control | PASS |
| Claim/evidence consistency | `assertClaimEvidenceConsistency` + verifier lease/dirty-worktree rules | `RUNNING`/empty, `PLANNED`/non-empty, ACTIVE owner without session; PLANNED + dirty worktree; PLANNED + ACTIVE lease | claim matrix | PASS |
| Credential-shaped content rejected without echo | `containsSecret` over the raw spec text before parsing | `ghp_...` value in the spec | secret control (asserts no fragment in the report) | PASS |
| Credits exactly once, appends once, preserves existing streams | `registry.revision = expectRevision + 1`; single `push`; prior streams byte-compared | — | positive control | PASS |
| Deterministic render on repeated runs | `renderRegister` + `verifyRenderedBytes` | two independent in-process runs at a pinned `now` | determinism control | PASS |
| One winner / one typed loser for concurrent creators | lock + CAS | two real CLI creators in a real temp repo at the same revision | concurrency control | PASS |
| Fault atomicity; `STAGE_RECEIPT` unreachable | staged publish + `finally` discard | `AFTER_LOCK`, `STAGE_STATE`, `STAGE_RENDER`, `VERIFY_RENDERED`, lock contention, `STAGE_RECEIPT` | fault matrix | PASS |
| CLI usage: missing/duplicate `--stream-spec`, prohibited flag | CLI `parse()` + `create-stream` required-flag pre-check | `USAGE_MISSING_STREAM_SPEC`, `USAGE_DUPLICATE_FLAG`, `USAGE_UNKNOWN_FLAG` | CLI matrix | PASS |
| New streams appear through the canonical registry only | `status.mjs` reads `doc.streams`; `registerSnapshot` derives from `streams[]` | snapshot size must equal `streams.length` | status/snapshot control | PASS |
| Stale seed pin is detected | `seed.test.mjs` exact 12-id inventory + count pin | pre-reconciliation inventory (8 ids) must fail the pin; an unregistered extra must fail | `node --test ops/workflow/__tests__/seed.test.mjs` | PASS |
| Reconciliation through tooling, never a hand edit | 4 x `create-stream` + 1 x `coordination-update` | every rejection path leaves state/render/receipt byte-identical | transitions above; `git status` on the registry | PASS |
| No new byte-hashed artifact class | created records carry `artifacts: []`, `closure: null` | not applicable (nothing pinned) | `check-attr` + record inspection | PASS (N/A) |

## 4. Gate tally

Mandatory total **8** / passed **8** / blocked **0** / unperformed **0**.

| # | Command | Exit | Result |
| --- | --- | --- | --- |
| 1 | `npm run workflow:test` | 0 | 244 tests / 244 pass / 0 fail (baseline was 230; +14 new) |
| 2 | `npm run workflow:verify -- --state docs/plans/atlas-delivery-cycles.json` | 0 | `status=ok`, 12 streams, `errors=[]` |
| 3 | `npm run workflow:render:check` | 0 | `status=ok`, no drift |
| 4 | `git diff --check` | 0 | clean |
| 5 | `npm run workflow:status -- --json` | 0 | reads the reconciled registry; `registerRevision=22`, coordination `CYCLE_ACTIVE`, zero sessions (empty store is absence of evidence, not inactivity) |
| 6 | `node --test ops/workflow/__tests__/create-stream.test.mjs` | 0 | 13/13 |
| 7 | `node --test ops/workflow/__tests__/seed.test.mjs` | 0 | 5/5 including the stale-pin control |
| 8 | Reconciliation + `verify` + `render:check` on the reconciled tree | 0 | revision 17 -> 22; `verify` clean; render regenerated |

## 5. Observation table (`origin/main` tip `9b82a98f7c74ffecd34e2dae337d7a69c19d7fdf`)

Registered as ACTIVE (unintegrated work and/or an in-progress write, plus
ownership/activity evidence):

| Worktree | Branch | HEAD | Ahead | Dirty | Last commit | Verdict | State |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `E:/ATLAS-worktrees/beneficiary-export-parity-c05` | `work/beneficiary-export-parity-c05` | `691a7c4a` | 20 | clean | 2026-09-15 13:05 +08 | ACTIVE — frozen handoff `REVIEW_REQUIRED` (23/23/0/0) | `REVIEW_REQUIRED` |
| `E:/ATLAS-worktrees/companion-sso-live-prep-c02` | `work/companion-sso-live-prep-c02` | `233ae67f` | 7 | clean | 2026-09-15 13:36 +08 | ACTIVE — handoff `REVIEW_REQUIRED`, `PREPARED_WITH_EXTERNAL_GATE` | `REVIEW_REQUIRED` |
| `E:/ATLAS-worktrees/tl-operator-workspace-c05` | `work/tl-operator-workspace-c05` | `05bb8e51` | 12 | clean | 2026-09-15 14:23 +08 | ACTIVE — frozen handoff `REVIEW_REQUIRED` | `REVIEW_REQUIRED` |
| `E:/ATLAS-worktrees/workflow-atomic-stream-registration-c04` | `work/workflow-atomic-stream-registration-c04` | `9b82a98f` | 0 | clean | 2026-09-15 14:14 +08 | ACTIVE — this WF-C04 cycle (advertised packet) | `RUNNING` |

Excluded:

| Worktree | Reason |
| --- | --- |
| `E:/ATLAS-worktrees/stakeholder-export-parity-audit-c05` | Clean; its tip `2e0b406f` is an **ancestor** of the active `beneficiary-export-parity-c05` tip `691a7c4a` (`git merge-base --is-ancestor` exit 0) — the audit lane was absorbed into the continuation, not running |
| `E:/ATLAS-worktrees/workflow-next-prep-20260915` | Packet-storage/authoring branch: `git diff --name-status origin/main...codex/workflow-next-prep-20260915` adds only `docs/prompts/**` and `docs/reference/**` documents and no product/test file (the prompt count is observation-boundary-dependent — 4 prompts + 1 reference document at this executor's observation, 5 at the planner's integration observation; docs-only either way); no writer, no handoff — it is the *input* to cycles, not a running lane |
| `E:/ATLAS-worktrees/enrollpro-proxy-recovery-live-20260915` | HEAD == `origin/main` (0 ahead, no unintegrated work); its stream is already registered as `ENROLLPRO-PROXY-RECOVERY-LIVE` (`EXTERNALLY_BLOCKED`) and the packet is NOT GRANTED |
| `E:/ATLAS-worktrees/integration-workflow-c02-c03-20260914` | HEAD `53a781a4` is an ancestor of `origin/main`; already integrated, no unintegrated work |
| `E:/ATLAS-worktrees/workflow-hardening-c02` | HEAD `cf1d360c` is an ancestor of `origin/main`; `WF-C02` is already `COMPLETE` |
| `D:/ATLAS-worktrees/*` (legacy root) | Legacy-retention root; every worktree there is registry-terminal, superseded, retirement-preserved, or runtime/deployment-owned. The two with unregistered local work are historical: `smart-ux-audit-c01` (last commit 2026-09-12, clean) and `teaching-load-tlc02` (last commit 2026-09-09, only untracked fixture leftovers). None has a current-cycle advertised packet or recorded session |
| `D:/ATLAS-runtime-*`, `D:/ATLAS-runtime-supervised-3d916b26-20260912`, other `D:/ATLAS-*` runtime dirs | Runtime/deployment-owned shared surfaces; never registered as product lanes and never touched |
| D-drive worktrees reported `dubious ownership` by Git | Unobservable read-only (owner `BUILTIN/Administrators`); all are historical/integrated/runtime-preserved worktrees. `safe.directory` was deliberately **not** added — that would mutate global Git config |

Created stream records (all via `create-stream`, never hand-edited):

| Stream id | Kind | State | Candidate | `changedPaths` | Observation SHA | Observed at (UTC) |
| --- | --- | --- | --- | --- | --- | --- |
| `BENEFICIARY-EXPORT-PARITY-C05` | STREAM | `REVIEW_REQUIRED` | `691a7c4a` | 35 | `9b82a98f…` | 2026-09-15T06:35:56.428Z |
| `COMPANION-SSO-LIVE-PREP-C02` | STREAM | `REVIEW_REQUIRED` | `233ae67f` | 6 | `9b82a98f…` | 2026-09-15T06:35:58.408Z |
| `TL-OPERATOR-WORKSPACE-C05` | STREAM | `REVIEW_REQUIRED` | `05bb8e51` | 26 | `9b82a98f…` | 2026-09-15T06:36:00.163Z |
| `WF-C04` | CYCLE | `RUNNING` | null | 0 | `9b82a98f…` | 2026-09-15T06:36:02.454Z |

`coordination-update --mode CYCLE_ACTIVE --active-cycle-id WF-C04` then set the
global next action to the WF-C04 critical path and recorded that
`ENROLLPRO-PROXY-RECOVERY-LIVE` remains `EXTERNALLY_BLOCKED` pending an elevated
execution context (revision 22).

**Remaining unregistered active lanes: 0.**

Two prepared-but-ungranted HIGH packets (`companion-sso-migration-live-c02`,
`companion-sso-runtime-activation-c02`) are named inside the
`COMPANION-SSO-LIVE-PREP-C02` record rather than as separate streams: they own no
worktree and no unintegrated work of their own, so they are approval gates, not
running lanes. This is a disclosed discretionary call — see risks.

## 6. Assertion inventory

- Added: 13 tests in `ops/workflow/__tests__/create-stream.test.mjs`; 1 test in
  `ops/workflow/__tests__/seed.test.mjs` (the stale-pin negative control).
- Modified: `seed.test.mjs`'s total pin (`8` -> `SEED_STREAM_IDS.length`, 12) and
  its explicit id list are now the exact 12-id inventory, compared as an ordered
  set so a failure names the missing/extra ids.
- Removed: none. No existing assertion was weakened, deleted, or skipped. The
  `workflow:test` count moved 230 -> 244 with zero losses.

## 7. `.gitattributes` / artifact-pinning result

This cycle pins **nothing new**. Evidence checked: all four created records carry
`artifacts: []` and `closure: null` (no new byte-hashed class exists); the
existing `artifact-portability.test.mjs` is unchanged and still passes over every
pinned artifact; and `git check-attr eol` resolves `lf` for every new or touched
path class (`ops/workflow/specs/register/*.json`, `ops/workflow/__tests__/*.mjs`,
`docs/handoffs/**`, `docs/plans/**`). No `.gitattributes` change was required.

## 8. Custody-summary deferral

The WF-C03 successor note's optional redacted read-only custody summary is
**explicitly deferred** by this packet. It was not implemented, and no
browser-session ownership was committed into machine state. No browser was
driven, no login occurred, and no custody lease was acquired.

## 9. Boundary / zero-mutation statement

No deployment, no runtime/task/env change, no listener on 5001/5174, no
live/shared database access or mutation, no migration, no term-cache action, no
Teaching Load write, no generation, no publication, no browser login, no
companion-repository edit, no worktree creation or retirement, no `git push`,
`merge`, `rebase`, or `force`, and no edit outside this worktree. The only Git
network operation was the read-only `git fetch origin` used for observation. No
credential was read, written, echoed, or committed.

## 10. Known risks / residuals

- **NON_BLOCKING (pre-existing, not introduced here):** `--now` is advertised in
  the CLI `BASE_FLAGS` but rejected by the engine's applicability loop
  (`TRANSITION_FLAG_NOT_APPLICABLE`) for **every** transition, including
  pre-existing ones — demonstrated on `coordination-update` at base. The
  determinism control therefore pins time through the engine's `now` parameter
  instead of the CLI. Not fixed: widening the accepted-flag surface of unrelated
  transitions is outside this packet's scope.
- **NON_BLOCKING (disclosed discretionary call):** the two prepared companion
  HIGH packets are recorded inside `COMPANION-SSO-LIVE-PREP-C02` rather than as
  their own `HIGH_APPROVAL_REQUIRED` streams. A planner may prefer separate
  records (matching the `ENROLLPRO-PROXY-RECOVERY-LIVE` precedent); that is a
  one-command `create-stream` addition if desired.
- **NON_BLOCKING:** the D-drive legacy worktrees could not be inspected where Git
  reports `dubious ownership`. Those rows are reported as unobservable rather
  than assumed inactive; all are historical, integrated, or runtime-owned.
- **NON_BLOCKING:** the `RUNNING`/`PLANNED`/`ACTIVE`-owner claim rules are
  **creation-time-scoped**. They are enforced only by `create-stream` on the
  registered record, not globally. A stream-scoped transition that accepts
  `--running`/`--awaited` can change those lists afterwards — `lease-update`
  lists both in its `optional` set, and the engine applies
  `applyAwaitedRunning` for every stream-scoped transition — so the invariant is
  not bypassable at registration but is **not** a document-wide rule. This is
  pre-existing behavior and out of scope for the WF-C04 correction; a
  document-wide rule would require a verifier change and is not claimed here.
- **NON_BLOCKING:** created streams record `owners.*.sessionId = null` because no
  session identifier was observed. The executor deliberately did not invent ids,
  so heartbeats cannot yet be joined to these records by session.

`EXECUTOR_SESSION_ROUTE: EXISTING` — bounded corrections return to this task if
the planner resumes it.

---

## 11. R1 correction round — created-stream integration lifecycle

**Risk tier:** MEDIUM. **Verdict:** `REVIEW_REQUIRED`. Additive commits only;
`docs/plans/atlas-delivery-cycles.json` and the generated register are
**byte-unchanged** in this round.

### 11.1 Defect

The accepted candidate `6dc272bb` was merged for integration (merge `ff46ca46`,
parents `9b82a98f…` + `6dc272bb…`; `ff46ca46^{tree}` == `6dc272bb^{tree}`, so the
merge tree is byte-identical to the reviewed candidate). The integration
lifecycle then failed on the created-stream path:

```
record-integration --stream WF-C04 --integration ff46ca46…  -> TRANSITION_RESULT_INVALID
"candidate state failed verification: REMOTE_OBSERVATION_INVALID:
 integrationSha ff46ca46… is not an ancestor-or-equal of observed remote sha 9b82a98f…"
path: streams[11].git.remoteObservation.sha
```

The earlier `ACTIVE_CYCLE_TERMINAL` rejection in the same lifecycle was correct,
documented behavior (coordination had to move to `MANUAL` first, per the README
closure sequence) and is not part of this defect.

### 11.2 Root cause

`create-stream` stores `git.remoteObservation` as the tip observed *before* the
stream was integrated. The verifier requires `integrationSha` to be an
ancestor-or-equal of `remoteObservation.sha`. `record-integration` is the first
transition that sets `integrationSha`, and `record-remote-observation` is gated
to `INTEGRATED`/`COMPLETE`, so no transition could refresh the observation in the
eligible `ACCEPT_READY`/`INTEGRATION_READY` window. Every stream created by
`create-stream` was therefore un-integrable — including `WF-C04` itself and the
three reconciled lane records. The original trace table exercised
`create-stream` in isolation and never drove a created stream through
integration, which is exactly the gap that let this pass.

### 11.3 Fix

`ops/workflow/lib/transition.mjs`, `record-integration` only:

- New optional flags `--observed-remote <40-hex lowercase>` and `--observed-ref
  <ref>` (default `refs/remotes/origin/main`).
- With `--observed-remote`: the sha must exist as a commit
  (`TRANSITION_OBSERVED_REMOTE_UNKNOWN`), match the 40-hex lowercase shape
  (`TRANSITION_OBSERVED_REMOTE_INVALID`), and the integration must be an
  ancestor-or-equal of it (`TRANSITION_OBSERVED_REMOTE_ANCESTRY`). On success
  `stream.git.remoteObservation` becomes `{ ref, sha, observedAt: <transition
  time>, kind }` with the kind derived exactly as `record-remote-observation`
  derives it, and the refreshed snapshot is echoed in `summary.observation`.
- Without the flag, behavior is unchanged: a stream with `remoteObservation:
  null` integrates exactly as before and no observation is reported.
- `create-stream`'s storage semantics are unchanged; `record-remote-observation`'s
  `INTEGRATED`/`COMPLETE` gate is unchanged (a control asserts it still rejects
  from `ACCEPT_READY`); `coordination-update`, the schema, and the generated
  register are untouched.

### 11.4 Controls (new file `ops/workflow/__tests__/stream-integration-observation.test.mjs`, 7 tests)

| Control | Type | Result |
| --- | --- | --- |
| Creation-time observation + `record-integration` **without** the flag → `TRANSITION_RESULT_INVALID` / `REMOTE_OBSERVATION_INVALID`, revision unchanged, state and render byte-identical | failing-first | PASS |
| Full lifecycle on a disposable repo through the **real CLI**: `create-stream` (observation at tip A = `candidateSha`) → `record-executor-return` → `record-qa-result` ACCEPT_READY 1/1/0/0/0 → `record-integration --observed-remote <integration>` → `INTEGRATED`, revision 5, observation refreshed, `summary.observation` echoed, `verify` clean | positive | PASS |
| `--observed-ref refs/heads/main` → `kind: LOCAL_REF` | positive | PASS |
| malformed / uppercase / unknown / orphan / ancestor-only observations → typed `TRANSITION_OBSERVED_REMOTE_*`, zero state and render mutation | negative | PASS |
| `--observed-remote` on `create-stream` → `TRANSITION_FLAG_NOT_APPLICABLE` | negative | PASS |
| `remoteObservation: null` stream integrates without the flag, `summary.observation` absent, observation stays null | regression | PASS |
| `record-remote-observation` still rejects from `ACCEPT_READY` (`TRANSITION_INVALID_STATE`) | regression | PASS |

The observation sha **may equal** the integration sha: observing the integration
commit itself is a valid downstream-or-equal snapshot, and the verifier treats
equality as satisfying the ancestor-or-equal rule. The positive control asserts
exactly that case.

### 11.5 R1 gate tally — mandatory 6 / passed 6 / blocked 0 / unperformed 0

| # | Command | Exit | Result |
| --- | --- | --- | --- |
| 1 | `npm run workflow:test` | 0 | 251 / 251 / 0 fail (244 + 7 new) |
| 2 | `npm run workflow:verify -- --state docs/plans/atlas-delivery-cycles.json` | 0 | `ok`, 12 streams, `errors: []` |
| 3 | `npm run workflow:render:check` | 0 | `ok`, no drift |
| 4 | `git diff --check` | 0 | clean |
| 5 | `node --test ops/workflow/__tests__/stream-integration-observation.test.mjs` | 0 | 7 / 7 |
| 6 | `git diff 6dc272bb...HEAD -- docs/plans/` | empty | registry files byte-unchanged in this round |

### 11.6 R1 assertion inventory

Added 7 tests; modified none; removed none. Suite count 244 -> 251 with zero
assertion losses.

### 11.7 R1 corrections to the original handoff text

- **Section 10, claim rules:** the original sentence said the creation-time claim
  rules were non-bypassable because "no such transition exists today". That was
  **false**: `lease-update` lists `running`/`awaited` in its `optional` flags and
  the engine applies `applyAwaitedRunning` for every stream-scoped transition.
  Restated as creation-time-scoped, matching the code and the README.
- **Section 5, prep-branch exclusion:** the note asserted "exactly 4 paths". Path
  totals for a branch are observation-boundary-dependent (4 prompts + 1 reference
  document at this executor's observation; 5 at the planner's integration
  observation). Restated boundary-safely; the exclusion conclusion is unchanged.

### 11.8 R1 boundary statement

No registry transition was run in this round. No deployment, runtime/task/env
change, database, migration, term-cache, Teaching Load, generation, publication,
browser, companion, or push/merge/rebase action occurred. Only
`ops/workflow/**` and `docs/handoffs/wf-c04-executor.md` changed.
