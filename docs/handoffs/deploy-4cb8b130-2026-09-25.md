# Deploy handoff — `4cb8b130` (SERVER-STALL-C01 diagnostics), 2026-09-25

**For a fresh elevated planner/deployer session.** One page; do not reconstruct the prior session.

## Authority and status

- **HIGH deployment.** Operator has stated the target and the go; the pre-action review below passed.
- **Live now:** `ad8f9717` (`E:\ATLAS-runtime-supervised-ad8f9717-20260925`; machine
  `ATLAS_RUNTIME_SOURCE_DIR`/`_RELEASE_SHA` = that dir; the running `cli.mjs start` runs from there).
- **Target:** `4cb8b130` (`origin/main` was `949d2992`; `4cb8b130` is an ancestor; post-target delta is
  docs-only — `docs/prompts/ux-audit-size-c01-2026-09-25.md`).
- **Rollback:** `ad8f9717` (startable in place).
- **No migration** — `git diff --name-only ad8f9717 4cb8b130` touches no `prisma/`, migration, schema or
  `.sql` path; the applied migration count stays unchanged.

## Change (diagnostics only)

`atlas-server/src/lib/request-timing.ts`, plus a new `atlas-server/src/__tests__/request-timing.test.ts`:

- Open `text/event-stream` responses (notification SSE, long-lived by design) are **counted as
  `streams=N`** and are no longer listed as active work, counted in `inFlight`, or logged as slow
  requests. On the live logs they filled every stall line and hid the request that actually blocked.
- Stall lines gain `heap=<used>/<limit>MB` so a GC pause can be told apart from handler CPU work.

Also in the range but not the point of the release: `atlas-client/src/ui/accordion.tsx`,
`atlas-client/src/components/timetable/simple/SimpleMoreMenuContent.tsx`, and docs.

## Preflight (before cutover)

1. Create a clean detached release worktree at exactly `4cb8b130`, e.g.
   `E:\ATLAS-runtime-supervised-4cb8b130-20260925` (`git -C D:\ATLAS worktree add --detach <path>
   4cb8b130…`). Verify clean HEAD.
2. Install its **own** dependency tree (no `node_modules` junctions): `npm ci` in the root,
   `atlas-server`, `atlas-client`. Generate Prisma from the repo-root schema. Build the server (`tsc`)
   and the client (`vite`) with `VITE_ENROLLPRO_URL=https://dev-jegs.buru-degree.ts.net`. Never print
   the env file's values.
3. Run the focused gate `request-timing` (`npm --prefix <target>\atlas-server test -- request-timing`),
   plus the client typecheck.
4. **Add and push a pending `## Live release` record naming target prefix `4cb8b130` and rollback
   `ad8f9717`** in `docs/plans/live-state.md` (docs-only commit to `main`). The guarded runner fails
   closed without it. Verify the prefix sits inside the `## Live release` section.
5. Dry-run first, then execute only after it is clean:

```powershell
& "E:\ATLAS-runtime-supervised-4cb8b130-20260925\ops\runtime\deploy-runner.ps1" `
  -TargetSha "4cb8b130..." `
  -TargetSourceDir "E:\ATLAS-runtime-supervised-4cb8b130-20260925" `
  -IncumbentSha "ad8f9717..." `
  -IncumbentSourceDir "E:\ATLAS-runtime-supervised-ad8f9717-20260925" `
  -EnvFile "D:\ATLAS-runtime-config\atlas-server.env"
```

Then repeat with `-Execute` once the dry-run proves target identity, incumbent machine identity,
supervisor lineage and the committed live-state gate (`mutates=false`, `secretsPrinted=false`).

## Verification after cutover

- `supervisor-state.json` `state=running`, `releaseSha=4cb8b130…`; machine env = target; listeners
  5001/5174 with one supervisor lineage.
- `/api/v1/health` 200; `/api/v1/health/ready` 200 with `database:"ok"`; one DB-backed read
  (`GET /api/v1/subjects?schoolId=1`) 200; Tailnet `https://njgrm.buru-degree.ts.net/api/v1/health` 200.
- Served client entry byte-identical to the built `atlas-client\dist\assets\index-*.js`; the superseded
  entry 404s.
- Then the operator runs **one draft swap preview on run 318** and reads the new stall line to see what
  is blocking the server. That is the acceptance row; record the result in the `## Live release` block.

## Risks / do-not

- No migration; no generation, publication, or live-data write is unlocked by this release.
- Do **not** touch other release directories, branches, or `D:\ATLAS-runtime-*`.
- `5c100ea6` is a built-but-undeployed staging dir
  (`E:\ATLAS-runtime-supervised-5c100ea6-20260925`) superseded by this target — a reclaim candidate, not
  a rollback basis. `4893cbde` remains the deferred standalone clone.
- `E:` was reclaimed to ~61 GiB free on 2026-09-25; re-check before the build (warn < 50 GiB, fail < 25 GiB).

## Known pre-existing (not this release)

- The client `GET /api/v1/runtime/context` was observed at ~4,015 ms in the live logs;
  `fetchEnrollProActiveSchoolYear` (`section-adapter.ts`) uses `AbortSignal.timeout(4000)` and swallows
  failure to `null`, which degrades the source state to "saved data". EnrollPro itself answers in
  ~250–450 ms. Separate lane, not fixed here.
