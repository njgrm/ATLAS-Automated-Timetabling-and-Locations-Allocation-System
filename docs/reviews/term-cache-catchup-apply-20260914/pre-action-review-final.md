# TERM-CACHE-CATCHUP-APPLY-REFRESH-C01 — fresh independent pre-action review (final)

- **Cycle:** WF-SEED-TERM-CACHE-TL-C06-20260916
- **Lane:** TERM-CACHE-CATCHUP-APPLY-REFRESH-C01
- **Reviewer role:** WAVE_COMPLETION_AUDITOR (fresh, read-only)
- **Reviewer task/session id:** `ses_f57bae9a6ffemcBiu4GjJJmS2x`
- **Model / reasoning variant:** `opencode-go/deepseek-v4.1-flash`, nearest supported variant; disclosed fallback tier for a HIGH pre-action pass.
- **Reviewed tip:** `8e8ff0082500d15252760079115c3d47c7e85be8` (= `origin/main`)
- **Corrected packet commit:** `bd6241db` (docs-only, `docs/prompts/term-cache-catchup-apply-2026-09-14.md`, +130/-58)
- **Previously rejected revision:** `9d54f7d6` (CORRECTION_REQUIRED 20/16/3/1, findings F-A..F-E)
- **Directive pin:** raw and LF-normalized `origin/main:AGENTS.md` SHA-256 `7663164608a330af5440a6e0ea1ffade20987b49a7bb0d939f3b50f1aa2df0a3`
- **Verdict:** `ACCEPT_READY`
- **Mandatory tally:** `20 / 20 / 0 / 0` (passed == total, blocked 0, unperformed 0)

## Closure of the blocking findings

| Finding | Closure |
|---|---|
| F-A rollback truth | Verified against `ops/runtime/lib/supervisor.mjs:351-373` and `ops/runtime/cli.mjs:136-137,93-103`: `ROLLBACK_UNAVAILABLE` fires only when `previous` is falsy; live `previous` is non-null but names the **same** release and `sourceDir` (`54dce67b`), so the guard cannot fire and nothing older is restored. Packet statement accurate. |
| F-B superseded stop rules | §4.1 stops only on §4.0-capture internal inconsistency, release/sourceDir divergence from the reviewed release, a listener with zero or multiple owners, or health/readiness failure; explicitly bars superseded PID rows as stop rules. |
| F-C equivalence base | §4.3 now compares `54dce67b..origin/main`; independently reproduced the seven apply-path files as an empty `--stat`. |
| F-D retracted narrative | No live P1001-causality claim (survives only as explicit retraction). Boot recovery reproduced and recorded as positive evidence. |
| F-E pre-state binding | §4.0 requires one atomic execution-window capture of all nine elements with a `PLANNER_DECISION_REQUIRED` divergence stop and an explicit no-adaptation rule. |

## Residual risks (all NON_BLOCKING, reviewer-classified)

- **R1** `syncActiveTermContractAuthority` is a pre-existing second `termContractCache` writer in the forbidden rollover/sync flow; the apply's Serializable transaction, in-transaction fingerprint re-check and CAS make a double-write fail closed.
- **R2** Preparation-base and Appendix A anchors still cite `84dd537b`; operative check runs against current `origin/main` and passes.
- **R3** Durable-env `keyCount: 14` not independently re-verified (credential file deliberately unread); consumed read-only at execution time.
- **R4** Proxy `/enrollpro-api/settings/public` observed 404: exactly the documented execution-window volatility.
- **R5** Minor tension between §4.0 item 1 and §4.1(a); harmless because no task change is authorized and a material task-identity divergence still stops the apply.

## Exact HIGH approval sentence (returned NOT GRANTED)

> I approve HIGH action TERM-CACHE-CATCHUP-APPLY-2026-09-14 against the live supervised ATLAS runtime only — the durable launch owner being the registered Windows scheduled task `\ATLAS-Runtime-Supervisor` (principal SYSTEM, schedule type at system start up, state Enabled, action `C:\Program Files\nodejs\node.exe "D:\ATLAS-runtime-supervised-54dce67b-20260914\ops\runtime\cli.mjs" start`, start directory `D:\ATLAS-runtime-supervised-54dce67b-20260914`) serving release `54dce67b8392cbce09aa810813c37f9c87a67159` from `D:\ATLAS-runtime-supervised-54dce67b-20260914` (product pin `d44f29e04d359ad9b18e4443b0fd4fed1daeaecd`), and against database `atlas_recovery_clean_rebuild_20260905` only, as follows: (1) let the live executor re-run the read-only preflight in §4, including the §4.0 atomic execution-window pre-state capture (registered task action/start directory/status; supervisor state `releaseSha`/`sourceDir`/`ownedPids`/`previous`; exactly one owning PID per port on 5001 and 5174; local health, local readiness with `database: ok`, and Tailnet health; `ROLLOVER_AUTO_SYNC_ENABLED=false`; EnrollPro direct origin and ATLAS proxy reachability; mirror 223 `term_contract_cache`/`term_contract_cached_at`/`is_active`/`is_archived`/`updated_at`; the recomputed live semantic revision, fingerprint, and exact confirmation text; and the `TERM_CACHE_SYNC_APPLIED` audit count), and stop before any login or write, returning `PLANNER_DECISION_REQUIRED` without adapting, re-deriving, or re-authorizing, if that capture is internally inconsistent or differs materially from the reviewed values — any ordered term, semantic revision, fingerprint, confirmation text, mirror identity or count, database pre-state, runtime `releaseSha`/`sourceDir`, listener ownership (zero or multiple owners), task identity, or rollover automation state — where the reviewed bindings are school 1 / EnrollPro school year 9 / mirror id 223, format TRIMESTER, ordered terms T1/T2/T3 (T1 `TERM 1` 2030-06-08→2030-09-15; T2 `TERM 2` 2030-09-16→2030-12-18; T3 `TERM 3` 2030-12-19→2031-04-08), `semanticRevision a51b62a26e27416c3d1295697f144d7ae5de5c56bb6a5e0d25bcb0c24dd8abb9`, fingerprint `d4cd7cc4466eb3390c802934204fca2fe01b8f80782478c7a6927b3041633f81`, and `confirmationText SAVE_TERM_AUTHORITY_1_9`; (2) authorize exactly ONE fresh local browser login at `https://njgrm.buru-degree.ts.net` for the named QA/session custodian (expected delta: exactly one `LOCAL_LOGIN_SUCCESS` audit row for the authorized actor plus that actor's `last_login_at` and that actor's engine-managed `updated_at` advance, and no other authentication side effect), whose authenticated actions are exactly the single `POST /api/v1/runtime/term-authority/apply` with `{"schoolId":1,"confirmationText":"SAVE_TERM_AUTHORITY_1_9","fingerprint":"d4cd7cc4466eb3390c802934204fca2fe01b8f80782478c7a6927b3041633f81"}` — the authorized apply changing exactly mirror `223.term_contract_cache`, mirror `223.term_contract_cached_at`, mirror `223.updated_at` (the engine-managed Prisma `@updatedAt` timestamp advancing on the non-empty `updateMany`), and exactly one school-1/year-9-scoped `TERM_CACHE_SYNC_APPLIED` audit row — plus the authenticated read-only acceptance GETs of §8.4 (TT-TL rows 4–5); (3) authorize the live executor to perform, only if that apply commits but the mandatory post-write verification fails, exactly one execution of the pre-reviewed guarded rollback transaction in §9 (raw SQL executed with `SET LOCAL TIME ZONE 'UTC'` and naive UTC `TIMESTAMP` literals; targeting only mirror id `223` / school 1 / EnrollPro year 9 with `is_active = true` and `is_archived = false`; requiring the persisted `semanticRevision` `a51b62a2…` and the exact post-apply `term_contract_cached_at`; restoring `term_contract_cache = NULL`, `term_contract_cached_at = NULL`, and the exact bound pre-apply `updated_at` `2026-09-10T11:18:14.634Z` (naive-UTC literal `2026-09-10 11:18:14.634`); requiring exactly one affected row and self-verifying inside the same transaction; retaining the immutable audit row; and aborting with zero changes and stopping on any guard failure) — and record that runtime rollback is NOT `cli.mjs rollback` but the ordered §11 procedure (stop the attempted operation safely; restore the §4.0 captured pre-action task/supervisor/release/sourceDir/listener/environment state; re-point the registered SYSTEM task to release `54dce67b` at `D:\ATLAS-runtime-supervised-54dce67b-20260914` only if the task action or start directory was changed; relaunch through the registered task `schtasks /run /tn ATLAS-Runtime-Supervisor`, ordered stop-then-start, never as a child of an attached shell and never worded as zero-downtime; then prove exactly one owner per port on 5001 and 5174, health and readiness 200, and task identity/action/start directory equal to the captured pre-action values); (4) forbid every action in §10 — no second login, no second apply or replay request, no rollover sync/archive, no Teaching Load carry-forward/suggestion/redistribution/apply, no timetable sync/edit/generation/publication, no migration or schema operation, no runtime deployment/restart/task/env change, no Tailscale or companion-repository mutation, no other database write, and no source/test/product change; and (5) require the post-apply acceptance matrix in §8 (read-back semantic parity, exact audit row, immutability proof that the only changed mirror-223 fields are the declared three plus the one audit row, TT-TL runtime-acceptance rows 4–5 re-run, and the canonical readiness diagnostic read-only with an honest typed blocker list), then return the evidence to the head planner, who must obtain a fresh post-action Wave Completion Auditor before closing TT-L runtime acceptance or unlocking generation or publication.

**Status: NOT GRANTED.** The apply is not authorized and was not executed. The sentence authorizes nothing until the operator returns it verbatim.
