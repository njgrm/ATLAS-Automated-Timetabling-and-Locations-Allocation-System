# Cycle packet — `PLAIN-LANGUAGE-J2J3-C01` (2026-07-26)

MEDIUM client-only source cycle. C3 J1/J4/J5 are already integrated on current `origin/main` but are
not deployed; this cycle closes the remaining J2 engine-token and J3 domain-jargon findings. No server,
schema, migration, dependency, deployment, browser, live-data, generation, or publication action.

## Worktree

- Base: current `origin/main` at dispatch.
- Worktree: `E:\ATLAS-worktrees\lane-a-plain-language-j2j3-c01`, branch
  `work/plain-language-j2j3-c01`, one writer.
- Dependency setup: use direct lockfile-verified dependency junctions to the preserved donor
  `E:\ATLAS-runtime-supervised-5c100ea6-20260925`; no npm ci/install and no junction chain. The executor
  records the donor and verifies root/server/client lockfile equality before running tests.

## In scope — presentation-only

1. Collapse the duplicate violation-label map: point `ScheduleReviewWorkspace.constants.ts` at the total
   plain `VIOLATION_TITLES` map while preserving totality, the neutral historical zone-imbalance label, and
   all typed unions.
2. Route `RightPanel` and unassigned-reason fallbacks through the existing humanising
   `resolveViolationLabel`/presentation helpers; no empty tooltip heading and no raw enum fallback.
3. Add total enum-to-plain-word maps for `RoomPreferenceDecisionStatus`, `RoomRequestAppealStatus`, and
   `GenerationRunStatus` in `lib/timetable-plain-language.ts`; apply them in the left rail, selected-entry
   panel, workflow dialogs, runs pane, and summary stats. Reuse the existing OfficerRoomPreferences pattern.
4. Soft-warning apply dialog: use the real plain violation presentation title/copy; keep the raw code in
   the existing tooltip/Expert affordance rather than deleting machine diagnostics.
5. `severitySummary` must import and reuse `MUST_FIX_LABEL`; do not hardcode a fourth copy.
6. Change the two false “All classes assigned successfully” paths to the canonical session wording; fix
   the three low-risk jargon strings (`algorithm`, `computing time`, `draft data`) without changing counts,
   scope, or authority.
7. Add failing-first, script-reachable tests T1–T7 from the reconnaissance: map parity, real production
   label rendering, enum humanisation, soft-warning copy, session-unit truth, total enum maps, and shared
   severity-label reuse. Use real fixtures/maps, not invented labels.

## Explicitly out of scope

`hard blockers` sentence authority and its 27 pinned occurrences; the Expert “Hard blockers” chip;
`isBlockingHardViolation` semantics; bare RightPanel HARD/SOFT counts pending an operator word decision;
severity-sign wording; `GR{n}` grade shorthand; all `showDiagnostics` content; typed/API unions;
`RunAvailabilityDriftCard`; audit findings 1/2/4/5/6/7/8/9/10; DRAFT-UX-C01 contracts; deployment.

## Acceptance rows

- T1/T2: the production label map and rail render the same plain title; the old terse title and raw code
  fail the control.
- T3/T6: no enum, SCREAMING_SNAKE, or underscore reaches the operator rail/entry/run/status surfaces;
  every union member maps to a non-empty plain label.
- T4: soft-warning dialog renders the real plain title and no `Soft Constraint Warnings`/raw code.
- T5: zero-unassigned surfaces never say “classes”; the canonical session wording is used.
- T7: summary severity label is derived from `MUST_FIX_LABEL`.
- Existing C3 gates remain green: `test:draft-ux-c01` 32/32, `test:warning-readability`,
  `test:timetable-relaxed-main`, `test:ux-guardrails`, and `test:client-suite` at the recorded 16-failure
  baseline with zero new failures. Typecheck and production build pass. Every new test file is named by a
  committed package script in the same commit.

One executor, one fresh QA, then planner integration from a clean boundary. QA must reproduce T1–T7
failing-first and classify every preservation failure against the base; no deployment evidence is claimed.
