# Deployment packet — `9f42190e` DRAFT-UX-C01 (Lane C, 2026-09-26)

HIGH supervised-runtime cutover (`AGENTS.md` §13). **Needs explicit operator approval for this packet**;
it does not waive evidence. No migration, generation, publication, or live-data write is authorized.

**Order (operator, 2026-09-26): sequential.** This packet runs only **after** the `861d89a2` cutover
(`docs/prompts/deploy-861d89a2-active-term-c02-2026-09-25.md`) is LIVE and its post-action QA is
`ACCEPT_READY`. If live is anything other than `861d89a2`, return `PACKET_STALE` before any mutation.

## Exact boundary

- **Target product:** `9f42190e720f4b50f77d706e77034662091f9f98` (`origin/main`, integration of
  `work/lane-c-draft-ux-c01` `1670a611`, QA `ACCEPT_READY` S1–S6 6/6).
- **Target release:** `E:\ATLAS-runtime-supervised-9f42190e-20260926` (detached registered worktree).
- **Incumbent / rollback:** `861d89a2bc2682c5f875dde0b4b1d8ffc079b1fe` at
  `E:\ATLAS-runtime-supervised-861d89a2-20260925`. Second-level rollback basis `eb0e3038` is the
  861d89a2 packet's, not this one's.
- **Dependency donor:** `E:\ATLAS-runtime-supervised-5c100ea6-20260925` (frozen; never copy
  dependencies from the production incumbent). No lockfile changed in `861d89a2..9f42190e`.
- **Behavioral production delta (`861d89a2..9f42190e`, excluding `docs/`):** 30 client files, +1606/−456 —
  `TimetableSimpleHeader`, `simple/SimpleHeaderActions` (new), `simple/SimpleSessionDetails` (new),
  `simple/SimpleHeaderHelpers`, `simple/SimpleMoreMenuContent`, `simple/SimpleBeneficiaryControls`,
  `ScheduleReviewWorkspace`, `TimetableGridConflictBadge`, `TimetableTaskDrawer`,
  `GeneratedUnassignedPanel`, `LeftRailContent`, `TimetableSimpleTypes`, `hooks/useTimetableState`,
  `atlas-client/package.json` (test scripts only) and client tests. **No server, Prisma, migration,
  SQL or lockfile change.**
- **Out of scope:** generation, publication, availability/Teaching Load writes, term-cache apply,
  rollover sync, companion repositories, EnrollPro reachability (`dev-jegs`), F1–F3, A3, data repair.

## Pre-action gates

1. One fresh `atlas-reviewer-high` reviews the immutable range `861d89a2...9f42190e` (confirm the
   delta above is exhaustive and client-only) and this packet's acceptance-satisfiability lint in one
   pass. `ACCEPT_READY` requires `passed == total`, `blocked: 0`, `unperformed: 0`.
2. Live is `861d89a2` with its post-action QA `ACCEPT_READY` (Live release block on `origin/main`).
3. The exact target is recorded in the `## Live release` block on `origin/main` before the runner.
4. The operator approves this packet explicitly in chat.

## Executor sequence

1. Re-derive task action, machine source/release, active supervisor state, and 5001/5174 listener
   lineage read-only. Require the exact incumbent `861d89a2`; return `PACKET_STALE` before mutation on
   drift.
2. Recheck capacity (E warns <50 GiB/fails <25; D warns <25/fails <15). **E: was 48 GiB free on
   2026-09-26** — below the warning; if still below 50 GiB, stop before creating the release and run the
   audited retention reclaim (`docs/reviews/runtime-dir-retention-20260925c/manifest.md` or its
   successor), then recheck. Check target path absent, donor existence/cleanliness/no borrower/
   non-reparse dependency trees/lockfile equality.
3. Add the exact detached target worktree. Copy donor root/server/client `node_modules` as real
   directories by the frozen donor procedure; no `npm ci`, implicit `npx`, or junction chain.
4. Run `npx --no-install prisma generate --schema ../prisma/schema.prisma` and the server build (no
   server source changed; build for the release only). Client, from `atlas-client/`: typecheck,
   `npm run test:draft-ux-c01` (10/10), `npm run test:client-suite` with the exact tally and every
   failure shown to reproduce on `861d89a2` (QA recorded 988 pass / 17 fail on the candidate, the same 17
   names as base; the "every client test file is named" check is among them), and the client build with the literal
   `VITE_ENROLLPRO_URL=https://dev-jegs.buru-degree.ts.net` build guard. Record that no component
   file exceeds 1000 lines.
5. Start the built target server directly on isolated port 5198 with `PORT=5198` and
   `ROLLOVER_AUTO_SYNC_ENABLED=false` set explicitly in the child environment. Require health 200,
   `/api/v1/health/ready` 200 with `database:"ok"`, and DB-backed `GET /api/v1/subjects?schoolId=1` 200.
   Stop the PID actually listening on 5198 and prove the port is clear. Never touch 5001/5174.
6. Capture the six-table zero-write SQL digest (exact SQL in the `861d89a2` packet's *Zero-write SQL*
   section, unchanged) immediately before cutover. Run the elevated SHA-pinned runner dry-run, verify
   exact target/incumbent, `mutates=false`, `secretsPrinted=false`, live-state gate, one lineage, and
   rollback, then repeat with `-Execute`.
7. Capture the post-cutover digest before any browser/data action. The runner is the only task/env/
   listener cutover owner. Do not improvise rollback.

Runner arguments for both dry-run and `-Execute` (the runner comes from the target release, pinned to
the target SHA):

```powershell
& "E:\ATLAS-runtime-supervised-9f42190e-20260926\ops\runtime\deploy-runner.ps1" `
  -TargetSha "9f42190e720f4b50f77d706e77034662091f9f98" `
  -TargetSourceDir "E:\ATLAS-runtime-supervised-9f42190e-20260926" `
  -IncumbentSha "861d89a2bc2682c5f875dde0b4b1d8ffc079b1fe" `
  -IncumbentSourceDir "E:\ATLAS-runtime-supervised-861d89a2-20260925" `
  -EnvFile "D:\ATLAS-runtime-config\atlas-server.env"
```

## Post-action rows

One fresh read-only `atlas-qa` closes Q1–Q7 with a real passed/blocked/unperformed tally:

| # | Row | Deciding harness |
|---|---|---|
| Q1 | Exact target live | Task action, machine env, active state, listeners, and one target `cli.mjs` lineage naming `9f42190e` / the target directory. |
| Q2 | Local runtime | `/api/v1/health` 200; `/api/v1/health/ready` 200 with `database:"ok"`; DB-backed `GET /api/v1/subjects?schoolId=1` 200. |
| Q3 | Tailnet | Tailnet health 200 with origin asserted. |
| Q4 | Served client is the target build | The served `assets/index-*.js` name and bytes equal the target build output. |
| Q5 | Startup evidence | Target log records start, listeners, DB/schema checks, and no fatal startup/listener/schema error. |
| Q6 | Zero live-data write | Pre/post six-table digests match (literal command, database name, serialization recorded; values not printed). |
| Q7 | Focused controls | Independently rerun `test:draft-ux-c01` 10/10 and `test:client-suite` with base-only failures separated. |

**D1 — browser acceptance (Lane C via `atlas-browser-qa` on Claude in Chrome, never the built-in pane),
at 1366×768 and 390×844, read-only, on the live draft (run 318, Term 2 unless the operator changed it):**

| # | Row |
|---|---|
| D1-S1 | Header shows ≤6 visible controls at 1366; exactly one primary (Generate with no run / Publish schedule with a run); Download schedules, School information and the other primary are reachable in More. |
| D1-S2 | No visible "Term" / "View type" text labels; the two dropdowns expose accessible names. |
| D1-S3 | A cell with a warning shows a severity sign (no "Schedule note ·" text); a Must-fix cell, if any, shows the Must-fix sign; hover/focus shows the note. |
| D1-S4 | At 1366 a warning cell opens a centred `role="dialog"` (not a bottom sheet), TIME card = day + start–end, one action row; at 390 the drawer opens. No global scrollbar. |
| D1-S5 | The unassigned entry is visible with a count for the selected term and opens the unassigned list (0 shown or disabled with a reason, never hidden). Read-only: do not place anything. |
| D1-N1 | QA NON_BLOCKING 1: on a plan-draft path with no timetable, the header's task-bound primary (e.g. "Open Teaching Load") is still reachable. `UNPERFORMED` if no such scope exists live — do not create one. |
| D1-N2 | QA NON_BLOCKING 2: the unassigned **count** equals the number of items in the opened list for the selected term (`TimetableSimpleHeader.tsx:470-474` uses raw `termFilter` for the count, `effectiveTermFilter` + reason filter for the list). Record both numbers. |

Deployment acceptance = Q1–Q7 `ACCEPT_READY`. D1 is UX acceptance; a D1 failure opens a correction
candidate, not a rollback, unless the page is unusable.

## Rollback

On runner failure, absent listener, target startup failure, identity drift, or digest mismatch, stop and
use only the runner's captured rollback path to `861d89a2`. Deploy-as-restore is a separate HIGH action
with its own evidence. No generation, publication, or data apply is part of this packet.
