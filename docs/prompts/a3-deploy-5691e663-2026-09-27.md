# A3 DEPLOY PACKET — release `5691e663` (client-only A3 UI/UX delta)

**Date:** 2026-09-27 · **Authority:** operator standing authorization for deploy + browser QA,
granted to Planner A3. Approval round-trip waived; **no gate is waived**.

> **STATUS: HELD AT A PRE-CUTOVER GATE. NOT DEPLOYED.**
> Gate **G1 (browser session) FAILS** — see §1.1. Lane A2 recorded a deliberate refusal to
> deploy this delta on 2026-09-27 for the same reason. The artifact is built, verified and
> ready; the cutover is one command away once a session exists.

---

## 1. Identity — resolved from machine scope and the task action, never from `Env:`

| | |
|---|---|
| **Live (rollback basis)** | `b0736007e89547ff66eab70d1d869e21f73d49ad` |
| Live source dir | `E:\ATLAS-worktrees\lane-a2-release-b0736007` |
| **Target** | `5691e663c5917cec8f29c33c530a9fcd01bb7978` (`origin/main`) |
| New release dir | `E:\ATLAS-worktrees\lane-a3-release-f426f465` (branch `release/a3-f426f465`) |
| Release worktree HEAD | `5691e663` — matches the target |
| Supervisor | scheduled task `ATLAS-Runtime-Supervisor` (SYSTEM), PID 14804, `state: running` |
| Live children | server PID 25644 → 5001, host PID 41332 → 5174 |

**Re-pin (review finding B2).** The first draft pinned `f426f465`. `origin/main` advanced
3 commits while the packet was under review: `b97706e3` and `5691e663` (both
`docs/plans/live-state.md`) plus merge `055bbd5a`. **The advance is docs-only** —
`git diff --name-only f426f465 5691e663` returns exactly one path,
`docs/plans/live-state.md`. The **product tree is byte-identical**, so the build in §4
remains valid and was **not** rebuilt. Per §16 a computed artifact is valid only for the
revision that produced it, so the pin is re-stated at `5691e663` and the release worktree
was fast-forwarded to match.

**Two traps detected and avoided.**

1. *Stale inherited env.* The agent shell inherits
   `ATLAS_RUNTIME_SOURCE_DIR=lane-a2-release-0da104f9` / `RELEASE_SHA=0da104f9` — **two
   releases stale**. Machine scope, the scheduled-task action, the supervisor command line
   and the active `supervisor-state.json` all agree on `b0736007`. Every `cli.mjs`
   invocation receives **explicit** env overrides. `productPin` (`d44f29e0`) is an ancestor
   floor and is **not** treated as live identity (confirmed a genuine ancestor of
   `b0736007`).
2. *Stale local branch.* `git rev-parse main` in `D:/ATLAS` returns `51563739`, **behind**
   `origin/main`. The target is read from `origin/main` after `fetch --prune`, never from
   local `main`.

### 1.1 Pre-cutover gate G1 — browser session (BLOCKING, currently FAILING)

Per §12 sessions are **seeded by the operator, never typed** by an agent, and a credential
value must never be echoed. A read-only probe was run against the live release:

```
GET https://njgrm.buru-degree.ts.net/sections  -> 302/redirect to /login
GET http://localhost:5174/sections              -> 302/redirect to /login
```

**Finding: `NEEDS_SESSION(space-bunny/default-profile)`** — no valid remembered session in
this browser profile on either the Tailnet or local origin.

**Why this blocks the cutover rather than being noted and proceeded past.** 11 of the 18
acceptance rows (B1–B11) are browser rows decidable only against a served, deployed build
with an authenticated session. Deploying without one produces a `DEPLOYED` outcome and
**zero** new acceptance evidence, while adding 11 unperformed rows to acceptance debt. A
healthy deployed process with no acceptance evidence is a worse state than a prepared
artifact awaiting one gate.

**Stop condition:** do not cut over until G1 returns a session. After seeding, re-probe,
confirm a protected route renders authenticated, then execute §6.

---

## 2. Expected delta — enumerated, not described

`git diff --name-only b0736007 5691e663` → **42 paths**.

| Area | Paths | Lane |
|---|---|---|
| `components/faculty-assignments/*`, `components/faculty/*`, `pages/Faculty.tsx`, `pages/TeachingLoad.tsx` | 17 | A3 S3 |
| `components/sections/*`, `components/BuildingView.tsx`, `pages/Sections.tsx` | 11 | A3 S1 |
| `components/subjects/*`, `pages/Subjects.tsx`, `lib/subject-constants.ts` | 8 | A3 S2 |
| `ui/select.tsx`, `components/admin-workspace/AdminWorkspace.tsx`, `atlas-client/package.json` | 3 | A3 shared |
| `docs/*` | 3 | docs only |

**`atlas-server/` changes: ZERO**, verified by filtering the 42 paths. No `prisma/`, no
`ops/`, no schema, no server route, no generation, no publication. The delta is
**client-only** and database state is untouched by this deploy.

> Correction to the first draft: it listed four shared files including
> `lib/grade-labels.ts`. **`lib/grade-labels.ts` is not in the 42-path diff** — S3 consumed
> the existing `components/GradeLevelBadge.tsx` and did not modify the labels module. The
> shared row is 3, as shown.

**Foreign-lane content, named explicitly.** One commit is not A3's:
`445fa39a docs(deploy): release b0736007 is live…`, which is Lane A2's own record of their
prior deploy. Verified docs-only — `git show --stat 445fa39a` lists exactly three files,
all under `docs/`. **No foreign product code is in this delta.**

**Commits above the live pin: 11.** The three A3 candidates `6ba386dc`, `13f1f189`+
`af239b49`, `f56ef29e`+`d608c62d`; **four** A3 merge commits (`af8ec0fd`, `f510c3f6`,
`0d39abb2`, `f426f465` — the first draft undercounted the merges); A2's `445fa39a` plus its
merge `3e5f51bd`.

**A3 review status, cited.** S2 Subjects `ACCEPT_READY` 36/36 passed, 0 blocked,
0 unperformed. S1 Sections/map `ACCEPT_READY` 10/10/0/0 on a bounded re-review, after a
first review returned `CORRECTION_REQUIRED` with two blocking findings. S3 Teachers/Load
source-accepted with four browser rows carried, not waived (U1–U4, closing at B7/B9/B8).

---

## 3. Rollback

Restore the supervised release `b0736007`, intact at
`E:\ATLAS-worktrees\lane-a2-release-b0736007`, by re-pointing the scheduled task and
restarting. **Verified a genuine restore, not a rebuild:** HEAD `b0736007`, working tree
clean, `atlas-server/dist/server.js` (3,070 B), `atlas-client/dist/index.html` (3,850 B),
and dependency trees present (client 155 entries, server 209).

> Correction: the first draft claimed the live release carried an untracked
> `atlas-server/.a2-counts.cjs`. **It does not exist** — `Test-Path` is False and the
> release's `git status --porcelain -uall` is empty. There is nothing to preserve and the
> rollback is unaffected.

**Rollback trigger:** any mandatory runtime row (R1–R7) fails; post-cutover health is not
200 on **both** `http://localhost:5001/api/v1/health` and `http://localhost:5174/`;
`GET http://localhost:5001/api/v1/subjects?schoolId=1` does not return 200; the D1–D5 proof
returns any combination other than the expected one; or the supervisor does not reach
`state: running` with `releaseSha = 5691e663`.

> Correction: the first draft's trigger read "health 200 on 5001 and 5174" without naming
> URLs. `5001 /` returns **404** on a healthy release, so an executor probing the bare port
> could have spuriously rolled back a good deploy. The exact URLs are now named above.

---

## 4. Pre-cutover verification already executed

| Step | Result |
|---|---|
| `git worktree add` at target | clean; later fast-forwarded to `5691e663` |
| A3 artifacts present in new release | `SubjectTermAuthorityBanner.tsx`, `homeRoomPersistence.ts`, `GradeBadge.tsx` **PRESENT** |
| Same artifacts in live release | all three **ABSENT** — the delta is real |
| Dependency trees | independent **copies** (client 155, server 209, **zero reparse points of any type**), donor unmodified. Not junctions: the live release is retirable under the retention policy and a junction to it would break this release |
| `prisma generate --schema <repo-root>` | exit 0, Prisma client 6.19.2 |
| server `tsc --noEmit --incremental false` | **exit 0** |
| server `npm run build` | exit 0, `dist/server.js` present |
| client `npm run build` | exit 0, 199 assets, `VITE_ENROLLPRO_URL` **process-local only** |
| EnrollPro origin in new bundle | `https://dev-jegs.buru-degree.ts.net` — identical to the live bundle |
| env leakage | `VITE_ENROLLPRO_URL` empty at process, Machine and User scope after the build |
| **capacity record** | E: **45.56 GiB** free, D: **39.44 GiB** free — both above the 25 GiB warning line |
| release worktree status | exactly one untracked path: this packet, now **committed**; `dist` and `node_modules` are gitignored |
| `package.json` delta | **test scripts only** — `test:a3-subjects`, `test:a3-sections-map`, `test:a3-teachers-load`. **No dependency change**, so the copied `node_modules` is valid |

---

## 5. Deploy discriminator — proven to differ BEFORE cutover

A healthy `/api/v1/health` on the old release is indistinguishable from a successful deploy.
Filename hashes are **rejected** as a discriminator: a full rebuild changes every asset
hash, so a hash difference is not attributable to any fix. The proof is **two-sided content
literals**, each verified present/absent in both builds *before* the switch.

| # | Literal | New build | Old build | Attributable to |
|---|---|---|---|---|
| D1 | `Refresh roster` | **present** (`Faculty-Ch0v0n_5.js`) | **absent** | S3 Fix 24 copy |
| D2 | `Refresh teacher roster` | **absent** | **present** (`Faculty-D-2MUhhm.js`) | S3 Fix 24 copy (replaced) |
| D3 | `subjects-form-result` | **present** (`Subjects-KlQigjVY.js`) | **absent** | S2 Fix 20 result region |
| D4 | `Show advanced` | **absent** | **present** (`Subjects-D2ciaXO0.js`) | S2 Fix 33A removed disclosure |
| D5 | `Keep queued` | **present** (`Sections-BpDKkcyN.js`) | **absent** | S1 Fix 12 queued-not-saved copy |

D1–D5 span all three A3 streams. Each is independently reproducible by
`Select-String -Path "<dist>\assets\*" -SimpleMatch -Pattern <literal> -List`.

**Post-cutover proof:** fetch the served asset over HTTP, require **status 200** and
`Content-Type: text/javascript` (an SPA fallback returning `text/html` would otherwise
produce a false "absent"), then assert D1, D3, D5 **present** and D2, D4 **absent**. Any
other combination falsifies the deploy and triggers rollback.

**Explicitly not used:** `atlas-server/dist/server.js` is a **3,070-byte stub** in the
rollback release. The directive records a planned discriminator that was exactly this stub
and was byte-identical across both builds — it would have reported "no difference" on a
deploy that genuinely changed another chunk.

---

## 6. Cutover — do not execute while G1 fails

1. Record pre-stop state: supervisor PID, child PIDs, `releaseSha`, `audit_logs` row count
   (the baseline R5 needs), and health on both ports.
2. Quiesce the **supervisor tree**. An out-of-process `cli.mjs stop` does not quiesce a
   resident supervisor — it respawns children. Kill the tree; settle ≥10 s.
3. Re-point scheduled task `ATLAS-Runtime-Supervisor` to
   `E:\ATLAS-worktrees\lane-a3-release-f426f465`. Requires an **elevated** shell; verify
   `IsInRole(Administrator)` before assuming the write landed.
4. Clear the new release's `supervisor-state.json` if it claims `running` (the
   `ALREADY_RUNNING` trap). Verified: the new release has **no** stale state file, so this
   step is currently a no-op.
5. Start the supervisor. Wait for `state: running` with `releaseSha = 5691e663`.
6. Verify listeners 5001 → new server PID, 5174 → new host PID.
7. Run the D1–D5 proof (§5) against the **served** bundle.
8. **Register update, corrected.** Update `docs/plans/live-state.md` with the new release
   SHA, directory, listeners and rollback basis — as a **targeted commit on a docs branch
   based on current `origin/main`**, preserving Lane A2's existing content verbatim.
   **Do not** edit it from the release worktree and **do not** dirty a live release
   directory. Reason: the release worktree originally sat at `f426f465`, whose
   `live-state.md` predates `b97706e3` and `5691e663` by 40 insertions / 14 deletions, and
   writing from it would have reverted the very lines recording Lane A2's acceptance blocker
   and their refusal of this deploy. A2's `NEEDS_SESSION` finding and their
   "deliberately NOT deployed" record **must be preserved**, not overwritten.

---

## 7. Acceptance rows — each names the harness that decides it

| Row | Claim | Harness | Type |
|---|---|---|---|
| **G1** | Authenticated session available in the browser profile | read-only probe of a protected route | **pre-cutover gate** |
| R1 | `releaseSha = 5691e663`, sourceDir = new release | `supervisor-state.json` + `schtasks` action | runtime |
| R2 | Health 200 on **both** `http://localhost:5001/api/v1/health` and `http://localhost:5174/` | `Invoke-WebRequest`, both exact URLs | runtime |
| R3 | **DB-backed** read returns 200 (health is liveness only) | `GET http://localhost:5001/api/v1/subjects?schoolId=1` | runtime |
| R4 | D1–D5 on the served bundle, with 200 + `text/javascript` | fetch served asset, string-probe | runtime |
| R5 | Zero unauthorized writes | `audit_logs` count captured in §6 step 1 vs after; delta 0. No migration, generation or publication call is issued | runtime |
| R6 | A2's publication work still served — server blobs untouched | `published-schedule.service.ts` blob parity vs `b0736007` | runtime |
| R7 | Zero residue: no stash/reflog rewrite, no untracked leftovers in the release | `git status --porcelain -uall` + reflog | runtime |
| **B1** | Subjects primary filters visible in one interaction | browser @1366×768 | **browser** |
| **B2** | BLOCKED term-authority state still `role="alert"` and loud; VERIFIED_LIVE compact | browser | **browser** |
| **B3** | Room-picker long occupant name readable, no clipping | browser | **browser** |
| **B4** | Room-map cards non-overlapping at 60/80/100 %; `Learning Commons` + `Guidance Office` full | browser | **browser** |
| **B5** | Cancel on a confirmed swap = **zero** changes | browser + network capture | **browser** |
| **B6** | Teaching Load density: >1 assignment row at 1366×768, no page scrollbar | browser | **browser** |
| **B7** | Fix 24 labels on one line, desktop **and** mobile menu, longest name | browser | **browser** |
| **B8** | Fix 23 pointerdown-outside closes the profile dialog | browser | **browser** |
| **B9** | Fix 14/16 density pixel assertion | browser | **browser** |
| **B10** | Select checked option legible **and** distinct from highlighted | browser | **browser** |
| **B11** | `Review teachers` modal returns to an unchanged roster | browser | **browser** |
| **B12** | **New — A2 cross-lane sweep.** A2's timetable surfaces consume the shared `ui/select.tsx` primitive that Fix 30 changed. Sweep the live timetable for any Select-state regression. | browser @1366×768 | **browser** |

B1–B12 are **deployment-acceptance clauses, not source rows**, and are labelled as such.
They cannot be decided by JSDOM and were never substituted with class assertions. U1–U4
from the source review close at B7, B9 and B8.

**Partial source coverage already closed, stated so no row is hidden.** B4's structural
half, B5's zero-write Cancel, B10's contrast and B11's return path are already decided by
the S1/S3 suites. Their **browser** halves — pixel overlap at real zoom, real pointer
interaction, real layout, and the A2 primitive sweep — are what remain.

**Sessions are seeded, never typed.** After the operator seeds, reuse the remembered
session. If any profile still lacks one, report `NEEDS_SESSION(<agent>/<profile>)` in one
line and continue with the other rows.

---

## 8. Explicitly out of scope

No migration, no `prisma db push` / `migrate reset`, no generation, no publication, no
Teaching Load apply, no term-cache apply, no schedule read or write.
`D:\ATLAS-runtime-config` is read-only to this packet. No credential is echoed, typed or
written anywhere.

---

## 9. Lane A2's recorded decline — planner position

Commit `5691e663` (2026-09-27), Lane A2's own register section, records:

> **Lane A3's merged delta is deliberately NOT deployed (2026-07-27 judgement)** … I am
> still declining it, because doing so would knowingly add 4 more unperformable rows to
> acceptance debt I already cannot close. **Ship it when the session exists, so the rows
> are decidable on arrival.**

**A3 does not override this.** A3 holds operator standing authorization for deployment,
and it could lawfully proceed. It does not, for two reasons: the recorded condition
("when the session exists") is **not met** — G1 fails; and deploying into that state
produces no evidence while enlarging acceptance debt, which is the outcome the condition
exists to prevent.

**The decline therefore stands, and is recorded here as agreeing with A2 rather than
superseding it.** If the operator directs deployment regardless, that is a supersession and
must be recorded as such, naming the standing authorization relied on and accepting the
11 unperformed browser rows as dated acceptance debt owned by Planner A3.

**Acceptance owner:** Planner A3, for B1–B12 against a deployed `5691e663`. Deployment and
acceptance are **separate outcomes**: a healthy deployed process is `DEPLOYED` even if
browser acceptance is incomplete.
