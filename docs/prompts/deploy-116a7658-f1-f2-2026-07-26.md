# Deployment packet — `116a7658` F1/F2 follow-up (2026-07-26)

HIGH supervised-runtime cutover for the isolated published-term/drift follow-up tip. The operator's
2026-09-20 standing authorization applies subject to every gate. This packet deliberately excludes the
unapproved Lane C DRAFT-UX product delta: the target is built from live `861d89a2` plus only the
reviewed F1/F2 commits.

## Exact boundary

- **Target product:** `116a765814bf56fdd30aec02c611869aaff42190` (verify full SHA from the target
  worktree before the runner; the product branch is `deploy/f1-f2-isolated-20260726`).
- **Target release:** `E:\ATLAS-runtime-supervised-116a7658-20260726` (detached registered worktree).
- **Incumbent / rollback:** `861d89a2bc2682c5f875dde0b4b1d8ffc079b1fe` at
  `E:\ATLAS-runtime-supervised-861d89a2-20260925`.
- **Dependency donor:** `E:\ATLAS-runtime-supervised-5c100ea6-20260925` (preserved donor; direct real
  directory copy, no junction chain).
- **Product delta from live:** exactly five paths — F2 drift map and its two client tests; F1
  `published-schedule.service.ts` and `published-immutability-c08.test.ts`. The range is a clean
  cherry-pick of reviewed F2/F1 commits onto the live product. It contains **no Lane C DRAFT-UX
  product path**, no client package script change, no server dependency change.
- **No migration / schema / SQL / lockfile change.**
- **Out of scope:** generation, publication, availability/Teaching Load writes, term-cache apply,
  rollover sync, browser UI mutation, companion repositories, and the separate Lane C `9f42190e`
  release packet.

## Pre-action gates and capacity

1. A fresh independent pre-action reviewer reviews the immutable range `861d89a2...116a7658`, the
   target exclusion proof, and this packet's acceptance-satisfiability lint in one pass.
2. The exact target is recorded in the `## Live release` block on `origin/main` before the runner.
3. Retention reclaim C is closed at `ACCEPT_READY` 17/17 but was discharged for the `861d89a2` build
   only. This is a new release build and E: is below the §3 50 GiB warning, so the retention obligation
   was live for it. The successor reclaim `docs/reviews/runtime-dir-retention-20260926a/manifest.md`
   (retire only `ad8f9717`) is **complete**: pre-action audit `ACCEPT_READY`, physical reclaim executed
   and recorded at `90eeb834` (only `ops/runtime/logs/` deleted, then non-forced `git worktree remove`
   + `git worktree prune`; no branch deleted; zero residue; keep set `861d89a2`/`eb0e3038`/`c5e167d7`/
   `5c100ea6`/`4893cbde` verified intact; live `861d89a2` unchanged). Measured post-reclaim **47.23 GiB
   free on E: and 60.67 GiB on D:**. **This discharge covers this single build only.** Because 47.23 GiB
   is still below the 50 GiB warning, any second release build — including `9f42190e` — re-triggers §3
   and requires its own fresh successor manifest and pre-action audit; the `9f42190e` packet's one-build
   deviation is not a substitute. Re-measure E:/D: immediately before the build and fail closed below
   E 25 GiB or D 15 GiB; the 50 GiB warning is the reclaim trigger, not the fail-closed build line.
4. Standing authorization permits execution after gates pass; stale identity or any failed gate stops.

## Executor sequence

1. Re-derive machine env, scheduled-task action, active state, listeners, and one live `861d89a2`
   supervisor lineage read-only. Return `PACKET_STALE` before mutation on drift.
2. **Add the exact detached target release worktree, then populate it.** Register
   `E:\ATLAS-runtime-supervised-116a7658-20260726` detached at
   `116a765814bf56fdd30aec02c611869aaff42190` with
   `git -C D:\ATLAS worktree add --detach "E:\ATLAS-runtime-supervised-116a7658-20260726" 116a765814bf56fdd30aec02c611869aaff42190`.
   If the path already exists, stop and return `PACKET_STALE` — do not reuse, repair, or force it.
   Then verify target worktree exactness and cleanliness (`rev-parse HEAD` equals the target full SHA;
   `status --short` empty), donor existence, lockfile equality, real dependency directories, and no
   borrower. Copy donor root/server/client `node_modules` as real directories using the frozen robocopy
   procedure; no `npm ci`, implicit `npx`, or junction chain. Retain the literal `git worktree add`,
   donor path, and robocopy invocation as run.
3. Run `npx --no-install prisma generate --schema ../prisma/schema.prisma`, server build, and focused
   F1/F2 gates: real-PG C08 **160 pass / 0 fail**, `test:server-db` with only the recorded base failures,
   published identity suites, F2 `test:publish-drift-revision-s4-client` (drift) plus
   `test:client-suite` (rendered), client scheduler-concern, client typecheck, and client build with
   literal `VITE_ENROLLPRO_URL`. Record exact tallies and base-vs-candidate classification.
4. Start the built target directly on isolated port 5198 with `PORT=5198` and
   `ROLLOVER_AUTO_SYNC_ENABLED=false` explicitly set in the child environment. Require health 200,
   ready 200 with `database:"ok"`, and DB-backed subjects 200; stop the actual listener PID and prove
   5198 clear. Never touch 5001/5174.
5. Capture the exact six-table zero-write SQL from the packet below immediately before cutover. Run the
   elevated deploy-runner dry-run, verify target/incumbent, `mutates=false`, `secretsPrinted=false`,
   live-state gate, one lineage, and rollback. Repeat identical args with `-Execute`; the runner is the
   only task/env/listener cutover owner.
6. Capture post-cutover digest before any browser/data action. Verify Q1/Q2/Q3/Q5/Q6. Fresh QA owns
   Q4/Q7/Q8.

Runner arguments:

```powershell
& "E:\ATLAS-runtime-supervised-116a7658-20260726\ops\runtime\deploy-runner.ps1" `
  -TargetSha "116a765814bf56fdd30aec02c611869aaff42190" `
  -TargetSourceDir "E:\ATLAS-runtime-supervised-116a7658-20260726" `
  -IncumbentSha "861d89a2bc2682c5f875dde0b4b1d8ffc079b1fe" `
  -IncumbentSourceDir "E:\ATLAS-runtime-supervised-861d89a2-20260925" `
  -EnvFile "D:\ATLAS-runtime-config\atlas-server.env"
```

## Post-action QA rows

| # | Row | Deciding harness |
|---|---|---|
| Q1 | Exact target live | Task action, machine env, active state, listeners, and one target `cli.mjs` lineage name `116a7658` / target directory. |
| Q2 | Local runtime | `/api/v1/health` 200; `/api/v1/health/ready` 200 with `database:"ok"`; DB-backed subjects read 200. |
| Q3 | Tailnet | Tailnet health 200 with origin asserted. |
| Q4 | Active-term readiness | Exactly two authenticated `GET /api/v1/generation/1/10/readiness/diagnostic` calls from the Tailnet origin using the seeded QA session; first `cached=false`, second `true`, `ran=true`, `zeroWrite=true`, no `/timetable` navigation or extra readiness call. |
| Q5 | Startup log | Target log clean: listeners, DB/schema verification, rollover disabled, no fatal startup error. |
| Q6 | Zero live-data write | Exact SQL pre/post digests match before Q4/browser activity. |
| Q7 | Focused controls | C08 **160 pass / 0 fail**; F1 published identity suites; F2 `test:publish-drift-revision-s4-client` (drift) + `test:client-suite` (rendered); scheduler-concern; server/client builds; base-only failures classified. |
| Q8 | Acceptance honesty | `ACCEPT_READY` only if all rows pass with `passed == total`, `blocked: 0`, `unperformed: 0`. This is deployment acceptance, not a browser UX or generation/publication acceptance. |

## Zero-write SQL

Run read-only through the durable env without printing values, with `SET TIME ZONE 'UTC'`; retain the
literal command, database, and serialization:

```sql
SET TIME ZONE 'UTC';
SELECT 'audit_logs' AS table_name, md5(COALESCE(string_agg(to_jsonb(x)::text, E'\\n' ORDER BY id), '')) AS digest FROM audit_logs x
UNION ALL SELECT 'generation_runs', md5(COALESCE(string_agg(to_jsonb(x)::text, E'\\n' ORDER BY id), '')) FROM generation_runs x
UNION ALL SELECT 'manual_schedule_edits', md5(COALESCE(string_agg(to_jsonb(x)::text, E'\\n' ORDER BY id), '')) FROM manual_schedule_edits x
UNION ALL SELECT 'published_schedule_revisions', md5(COALESCE(string_agg(to_jsonb(x)::text, E'\\n' ORDER BY id), '')) FROM published_schedule_revisions x
UNION ALL SELECT 'faculty_availabilities', md5(COALESCE(string_agg(to_jsonb(x)::text, E'\\n' ORDER BY id), '')) FROM faculty_availabilities x
UNION ALL SELECT 'faculty_availability_slots', md5(COALESCE(string_agg(to_jsonb(x)::text, E'\\n' ORDER BY id), '')) FROM faculty_availability_slots x
ORDER BY table_name;
```

## Rollback

On failure, use only the runner's captured rollback. Deploy-as-restore to `861d89a2` is a separate HIGH
action. Do not improvise task/env/listener changes. A nonzero digest, missing listener, or live-data
mutation is an incident stop.
