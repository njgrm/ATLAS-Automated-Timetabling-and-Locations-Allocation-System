# Prisma Bootstrap

This directory contains Prisma schema and migration assets for ATLAS.

## Quick Start

Run from project root:

- npm run db:generate
- npm run db:migrate

If you need schema sync without migration files:

- npm run db:push

Open Prisma Studio:

- npm run db:studio
